# 작업 기록 — 2026-07-01 19:47 (KST)

> 기록 ID: `20260701_194726_nav-selection-menu-ux`
> 이전 기록: `20260701_161500_file-selection-c3-draft`

## 1. 요청(연속)
- 커스텀 메뉴(초박형·hover 전환·ALT·사각 Border), 이동 기록(패널별)·버튼 동작·상위 이동, 네비 버튼 축소,
  실제 아이콘·더블클릭 진입, Explorer식 선택/호버(테두리·포커스아웃 회색), 키보드 위/아래, 클릭 지연 수정.

## 2. 구현
### NexaMenuBar (재사용 컨트롤, 이미 push됨: 289c911·5045e03·57147f0·87cefbd)
- 기본 MenuBar/MenuFlyout 한계(초박형 불가·둥근 팝업·hover 전환 불가) → **Button 헤더 + 경량 Popup(사각 Border)**.
- hover 전환, ALT 토글/단축키(문자), ESC·바깥 클릭 닫기, 사각 hover 하이라이트.

### 네비게이션 (패널/탭별)
- 패널별 뒤로/앞으로 스택 + 현재 경로(`Nav`). 뒤로·앞으로·**위로(상위 폴더)** 버튼 동작 + 활성상태 갱신.
- 더블클릭 진입도 기록. 네비 버튼 축소(22×22/폰트11, `NavBtnStyle`).

### 실제 아이콘 + 더블클릭 (이미 push됨: 25bb330)
- `DirItem.IconImage`(셸 썸네일 비동기), 글리프 폴백. 더블클릭 폴더 진입.

### 선택/호버 비주얼 (Explorer식)
- 호버=옅은 파랑, 선택(활성)=연한 파랑+파란 **테두리**, 선택(비활성/윈도우 비활성)=**회색**(포커스아웃).
- 테두리는 **항상 1px**(투명↔색만) → 선택 시 행 높이 점프 방지.
- 활성 패널 전환(`SetActivePanel`) + 윈도우 `Activated`(`_windowActive`) → `RefreshSelectionFocus`.

### 상호작용
- **클릭 지연 수정**: `Tapped`+`DoubleTapped` 조합이 더블탭 타임아웃(~300ms)만큼 Tapped를 지연 → 선택을 **`PointerPressed`**(즉시)로 이동, 디스클로저도 동일.
- **키보드 위/아래**: 그리드 포커스 상태에서 ↑/↓ 단일 선택 이동 + `NexaFileGrid.BringIndexIntoView`(스크롤).

## 3. 검증
- `dotnet build app/Nexa.App -c Debug` → **0/0**. 클릭 즉시 반응·높이 불변·키보드 이동·포커스아웃 회색 확인.

## 4. 다음(백로그)
- 드래그(러버밴드) 선택, 헤더 드래그 재정렬, 컬럼 설정 모달, 코어 가시행 스트림(C1), 설정 시스템(JSON),
  메뉴 실제 명령 연결, 단축키 밑줄(&F) 표시, 키보드 Shift+화살표 범위 확장.
