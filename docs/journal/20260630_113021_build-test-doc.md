# 작업 기록 — 2026-06-30 11:30:21 (KST)

> 기록 ID: `20260630_113021_build-test-doc`
> 이전 기록: `20260630_102235_devenv-bootstrap-verify`

## 1. 요구
- 각 **부문별 빌드/테스트 방법을 별도 문서로 정리**.
- **변경 이슈가 있을 때마다 갱신**하도록 규약화.

## 2. 반영 (Done)
- `docs/18-build-and-test.md` 신설: 코어(Rust)/WinUI 앱(C#)/라이선스 게이트(cargo-deny)/CI/tools(예정)
  부문별 빌드·테스트 명령(로컬=CI 일치), 전제·OS지원·요약표·머지 전 전체 점검.
- **갱신 규약 명문화**: 문서 상단 + `CLAUDE.md` §6 — "빌드/테스트 명령·도구·전제·산출물·OS지원이 바뀌는
  변경마다 같은 커밋에서 docs/18 갱신, 신뢰 원천은 CI".
- 인덱스 동기화: `STATUS.md` 문서 인덱스(18 추가), `docs/16` 범위 10~18.

## 3. 비고
- 신뢰 원천 = CI(`.github/workflows/ci.yml`): core(fmt/clippy/test) · license-gate(cargo-deny) · app(restore/build Release).
- 앱 테스트 프로젝트는 아직 없음 → 기능 단위에서 `Nexa.*.Tests` + `dotnet test` 추가 시 docs/18 갱신 예정.

## 4. 다음
- M0 첫 기능 단위: 인터롭 Rust↔C# 왕복 PoC(+테스트) → docs/18에 인터롭 빌드/검증 절차 반영.
