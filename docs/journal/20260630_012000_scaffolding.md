# 작업 기록 — 2026-06-30 (KST)

> 기록 ID: `20260630_012000_scaffolding`
> 이전 기록: `20260630_010900_dev-methodology`

## 1. 요구
- 진행사항 push + **스캐폴딩 진행**. 사용자 요청 범위 내 **자동 진행**(불필요한 확인 최소화).

## 2. 자동화/권한
- `.claude/settings.json`(커밋) — git 일반·cargo·dotnet·읽기전용 allow, 파괴적 명령은 ask.
  → 다른 PC에서도 동일 적용. 로컬 메모리에 읽기전용 명령 무확인 선호 기록.

## 3. 스캐폴딩 (docs/15 단위 — 각 단위=커밋)
1. `chore(repo)` Rust 코어 워크스페이스 — nexa-core/vfs/interop, deny.toml, rust-toolchain.
   **맥 cargo test green** (4 crates). interop은 cdylib + `nexa_abi_version()` C ABI.
2. `docs(license)` LICENSE.md(PolyForm Noncommercial) + THIRD-PARTY-NOTICES.
3. `chore(app)` WinUI 3 앱 스켈레톤(빈 윈도우, Windows 전용, WindowsPackageType=None).
4. `ci` GitHub Actions — 코어(mac/win) fmt+clippy+test, cargo-deny 라이선스 게이트, WinUI 빌드.
5. `chore(repo)` 재현성 — bootstrap.ps1, global.json, .vscode.

## 4. 검증
- 코어: `cargo fmt --check` + `cargo clippy -D warnings` + `cargo test` 모두 green(맥).
- 앱(WinUI): 맥 미빌드 → CI windows-latest/Windows PC에서 검증 예정.
- 툴체인: Rust 1.96.0, .NET 10.0.301 (맥).

## 5. 다음 단계 (M0 계속)
- 인터롭 PoC: C#에서 `nexa_abi_version` P/Invoke 왕복.
- nexa-vfs: 로컬 디렉터리 **스트리밍 열거**(초안→확장).
- WinUI: ItemsRepeater 가상화 리스트로 폴더 렌더 → 네비게이션.
