# 작업 기록 — 2026-06-30 01:06:31 (KST)

> 기록 ID: `20260630_010631_context-sharing`
> 이전 기록: `20260630_010143_licensing-confirmed`

## 1. 요구 (R14)
- 다른 PC에서 clone 시 **세션 정리/메모리 내용을 바로 공유**되게 문서화.
- **공개되면 안 되는 내용**은 클라우드 등 다른 방식으로 공유 가이드.

## 2. 반영 (Done)
- **`CLAUDE.md`(루트) 신설** — 이식용 프로젝트 메모리. Claude Code 자동 로드 → 새 PC/세션 즉시 인계.
  (프로젝트 정의·DR-1~5·핵심요구·아키텍처·개발환경·작업규약·오리엔테이션·다음단계)
- **`docs/14-context-sharing.md` 신설** — 공개=저장소(CLAUDE/STATUS/docs), 비공개=저장소 밖.
  비밀 커밋 금지 목록 + 공유 채널(GitHub Secrets·비밀번호관리자·private repo·git-crypt/sops·클라우드).
- **`.gitignore` 신설** — `.env`/`*.pfx`/`*.pem`/`secrets/` 등 비밀 패턴 차단(사고 예방).
- `README`/`STATUS` 인덱스·온보딩 갱신.

## 3. 핵심 정리
- Claude 로컬 메모리(`~/.claude`)는 clone 전달 안 됨 → 내용은 `CLAUDE.md`/`docs`에 반영해 공개 공유.
- 저장소 public → 비밀은 절대 커밋 금지, 별도 채널.

## 4. 다음
- 개발 방법론(작은 단위·단위별 커밋·점진 프로토타이핑) 문서화 → 별도 커밋.
