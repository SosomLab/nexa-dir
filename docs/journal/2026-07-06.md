# 2026-07-06 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).

## 🏷️ 릴리스 `0.2.0`
- `0.1.0`(C1 병합 전 베이스라인) 이후 M1 대규모 진행분을 묶어 **`0.2.0`** 태그·GitHub Release.
- 버전 동기화: 코어 워크스페이스 `Cargo.toml` 0.0.0→**0.2.0**, `Nexa.App`·`Nexa.Plugins` `<Version>`→**0.2.0**.
- 포함(0.1.0→0.2.0): 코어 트리/선택(C1)·성능(백그라운드 열거·범위 diff·아이콘 캐시·경로→NodeId)·구조(PanelView·`Nexa.ViewModels`)·**파일 조작 전체**(복사/이동/삭제[휴지통·완전]/이름변경/새로만들기·컨텍스트 메뉴·클립보드·DnD 전송엔진·덮어쓰기·바이트 진행률)·**컬럼 정렬**(3상태·다중열)·확장자/날짜시간 컬럼·**타입어헤드**·watcher 1차·탭 세션 저장·경로바 환경변수·**하단 패널**(정보·미리보기 텍스트/이미지·**ConPTY 터미널+VT**)·**미리보기 플러그인 SDK `Nexa.Plugins`(MIT, DR-6)**·문서 위키 Home.
- 아티팩트: MSIX/서명·포터블 zip은 **패키징 인프라 미비**로 이번엔 미첨부(소스/마일스톤 릴리스) — [12](../12-packaging-portable.md)·DR-3 후속. 알려진 이슈 [BUG-007/008](../BUGS.md).
- 빌드: 앱 x64 green(경고 4=터미널 미사용 필드, 에러 0).

## 전체 문서 통합 최신화 (다른 PC 병합분 정합)
- 다른 PC의 대규모 병합(1~3차 감사 + 하단 패널) 후 이 PC에서 저장소 최신화: 로컬 **main FF `1d9d312`→`fdd265b`(98커밋)**, 앱 빌드 green(경고 4=터미널 `VtScreen` 미사용 필드, 에러 0).
- **참조 문서 정합**(진행 문서 DEVLOG/MILESTONES/TASKS는 대체로 최신이었고, 지연된 기준·참조 문서를 맞춤):
  - `CLAUDE.md`: 현 단계(파일조작·컬럼·하단패널·터미널·미리보기 SDK 반영)·리포 구조(Nexa.Plugins/.Samples·Terminal/·Preview·ABI v7·docs 00~37)·**DR-6**·다음 단계.
  - `STATUS.md`: 상단에 07-06 하단 패널 블록(정보·미리보기 BP-2·ConPTY 터미널 BP-T·플러그인 SDK) 추가.
  - `MILESTONES.md`: 하단 패널 BP-2/터미널/플러그인 **☐→✅**(DEVLOG와 불일치 해소)·M2 🚧.
  - `docs/16`: §3 요약 카운트(코어 34·xUnit 57·ABI v7)·현재=main. `docs/19`: 카운트 갱신 + 신규 기능은 MILESTONES/DEVLOG 위임 규약 명시.
- docs/10(DR-6·ADR 색인)은 이미 최신이라 유지.

## 브랜치 기록 시간 역순 재정렬
- [BRANCHES.md](../BRANCHES.md)를 **시간 역순(최신 위)** 으로 재정렬(표·상세). 새 브랜치는 맨 위에 추가하는 규약.

## 내장 터미널 알려진 이슈 기록
- [BUG-007](../BUGS.md) 터미널 **캐럿(커서) 미표시** — `VtScreen`은 커서 위치 유지하나 렌더에서 커서 셀 미표시(BP-T3).
- [BUG-008](../BUGS.md) 일부 **SGR 색 미반영**(명령 미리보기 faint 등) — faint(2) 미적용·일부 시퀀스 미처리. 파워라인 글리프는 Nerd Font 별개(BP-T3).

## feat/bottom-panel-info → main 병합·삭제
- 하단 패널 콘텐츠(BP-2 정보/미리보기/플러그인 SDK + BP-T 터미널)를 main 병합(`3dd423a`). 코어·앱 빌드 green.
- 병합 후 `feat/bottom-panel-info` 로컬+원격 삭제. 병합 직후 린터가 csproj의 `Nexa.Plugins` ProjectReference를 제거한 것을 발견 → 폐기(복원)해 빌드 정상 확인 후 push.
