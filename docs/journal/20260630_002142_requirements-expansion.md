# 작업 기록 — 2026-06-30 00:21:42 (KST)

> 기록 ID: `20260630_002142_requirements-expansion`
> 단계: 설계 심화 (전체 목표 수집 → 아키텍처/언어 개발 전 확정 방침)
> 이전 기록: `20260630_001253_kickoff`

---

## 1. 이번 세션 목표

- **전체 목표를 수집**하고, **아키텍처·언어는 개발 전에 확정**해 진행(사용자 방침).
- 신규 핵심 요구를 설계에 반영.

## 2. 질문 & 답변 / 신규 요구 (Q&A)

### Q. 네이티브 언어 확정 방법
- **답변:** "전체 목표를 수집하고 아키텍처, 언어 등은 개발 전에 확정해서 진행."
- **반영:** 언어 즉답 보류 → 요구사항 수집 문서(`05`)와 언어 결정 ADR(`06`, 상태 Proposed) 작성.

### R1. (플래그십) macOS 스타일 인라인 폴더 확장 + 폴더 교차 다중 선택 ★ 최우선
- **요구:** 더블클릭으로 들어간 폴더에서 **하위 폴더를 인라인으로 펼쳐** 파일 보기,
  **서로 다른 폴더의 파일들을 동시에 다중 선택**(가장 중요한 포인트). (Finder 목록 보기 스크린샷 제공)
- **반영:** 전용 설계 `07-flagship-tree-multiselect.md` 신설, FR-X로 P0 승격,
  M1 범위 포함, ADR-0001 평가에 "트리/교차선택 적합성" 가중치 반영.

### R2. 좌/우 분리 패널 + 패널별 단일/멀티라인 탭
- **요구:** Double Commander / Path Finder 식 — 좌/우 패널이 각각 다른 디렉터리 탐색,
  **각 패널 상단에 싱글/멀티 라인 탭**으로 여러 폴더 동시 액세스.
- **반영:** FR-B(패널별 탭, 싱글↔멀티 라인 FR-B4), FR-C(패널마다 독립 탭 스트립) 갱신, 로드맵 M1.

### R3. Windows 기본 컨텍스트 메뉴 + 고유 메뉴 추가
- **요구:** 기본 Windows 컨텍스트 메뉴 사용 + Nexa 고유 메뉴 항목 추가 표시.
- **반영:** FR-J 신설(P0), `03-features §3-J`(IContextMenu/IExplorerCommand 호스팅 + 병합), 로드맵 M1.

### R4. 클라우드 / 네트워크 드라이브 연결 — 목표 설정, 차근차근 구현
- **요구:** 각종 클라우드/네트워크 드라이브 연결을 목표로, 단계적 구현.
- **반영:** FR-G 확장(SMB/UNC·매핑드라이브·WebDAV 추가), 단계(① SMB+SFTP → ② S3 → ③ 클라우드 SaaS),
  로드맵 M4를 "단계적 확장"으로 재구성.

## 3. 진행 사항 (Done)

- [x] `05-requirements.md` 신설 — 전체 FR/NFR/제약/가정/리스크/Open Decision/DoD.
- [x] `06-adr-0001-stack.md` 신설 — 언어/스택 상세 결정(평가 매트릭스), 상태 **Proposed**.
- [x] `07-flagship-tree-multiselect.md` 신설 — 플래그십 상세 설계·데이터 모델·수용 기준.
- [x] 기존 문서 동기화: `00-vision`(차별화 0순위), `02-roadmap`(M1 트리/탭/메뉴, M4 단계화),
      `03-features`(2-X 플래그십, 3-J 컨텍스트 메뉴, 패널별 탭), `README`(인덱스).

## 4. 확정/방침 (Decisions)

| # | 내용 |
| --- | --- |
| D5 | 플래그십(인라인 트리 + 교차 다중 선택)을 M1 P0로, "가시 노드 평면 스트림 + 가상화"로 구현 |
| D6 | 패널별 탭(싱글/멀티라인), 좌/우 독립 디렉터리 — Double Commander/Path Finder 식 |
| D7 | 컨텍스트 메뉴 = 셸 메뉴 호스팅 + 고유 항목 병합, 단일 액션 레지스트리 공유 |
| D8 | 클라우드/네트워크는 공통 Provider trait 우선, 단계적 확장(SMB+SFTP → S3 → SaaS) |
| D9 | 언어/스택은 **개발 전 확정** — ADR-0001 검토 후 사용자 승인 시 Accepted |

## 5. 미해결 / 결정 대기 (Open)

- [ ] **OD1** 네이티브 언어/스택 최종 확정 (권장 안 A: Rust 코어 + WinUI3). → `06`
- [ ] **OD2** AI 제공자/프라이버시 (M5 전).
- [ ] **OD3** 라이선스. **OD4** 패키징/배포. **OD5** Windows 빌드 환경.

## 6. 다음 단계 (Next)

1. ADR-0001 검토 → 언어/스택 확정(또는 비교 보강).
2. M0 스파이크 2종 설계 확정: ① 인터롭+10만 노드 가상 트리 60fps ② 교차 선택→혼합 작업.
3. 리포 스캐폴딩(`core/` Rust, `app/` WinUI, CI) → 개발 착수.
