# 2026-07-11 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).
> **⏱ 표기 = 해당 단위의 git 커밋 시각 범위(KST, 초 단위 원천=git log)** — 실제 착수는 첫 커밋 이전(직전 단위 종료 직후).

## 🔴 BUG-010 — 0.3.2 배포 자산 결함 2건 해결 (`fix/portable-render-xbf` 브랜치)

> 사용자 리포트: "포터블 다운로드 받아서 실행하면 깨져서 실행됨"(전 행이 `Nexa.App.DirItem` 텍스트). 상세 원인·교훈 = [BUGS.md BUG-010](../BUGS.md).

- **결함 A(렌더링)**: loose 소스 `.xaml` 폴백이 `x:Bind`를 죽임 → 게시 루트에 **컴파일 `.xbf`** 배치로 교체(`PublishLooseXamlAssets` 개편+XBF 미존재 빌드 에러 게이트, 스크립트 클린 산출). 스크린샷 검증.
- **결함 B(시작 크래시 0xC0000374, 30~60%)**: WER 덤프(탐지=CoreCLR delete) → **PageHeap full** 재현으로 범인 특정 = **WinAppSDK 1.6 MRT Core `GetDefaultPriFile`**(self-contained 한정, ResourceManager 생성 중 힙 손상). **WinAppSDK 1.6.\*→1.8.\***(csproj 4종)로 해소 — PageHeap 3/3·일반 5/5·Debug(FD) 스모크 green. `data\` 폴더 유무로 보였던 초기 패턴은 우연(비결정 재현으로 기각).
- **부수**: bootstrap.ps1·docs/18 런타임 표기 1.6→1.8(이 PC 1.8 기설치 확인) · 진단 도구(WinDbg·LocalDumps·PageHeap IFEO) 설치→분석→전부 원복.

## 🏷️ 릴리스 `0.3.2` — 설치기 동봉

- PKG-5 병합(PR#16 `9c242e5`) 후 버전 동기(Cargo+lock·csproj×2·lang `@app` → 0.3.2) → 태그 → CI `package` 잡이 **포터블 zip + setup.exe** 릴리스 자동 첨부.

## 클래식 설치기 setup.exe — CI 빌드·릴리스 첨부 (PKG-5, `feat/packaging-setup` 브랜치)

> 요구 흐름(사용자): "setup 파일 배포 구성" → "로컬 말고 GitHub Actions로" → "(로컬 Inno 제거) MSIX 서명 방법?" → "GitHub에서 exe 패키징 release"

- **경위**: MSIX(PKG-4)는 서명이 설치 필수 조건 — 서명 조사 결과 **Azure Artifact Signing(구 Trusted Signing)은 현재 개인=미국/캐나다만이라 한국 개인 불가**, 현실 경로=①Microsoft Store 제출(서명 위임, 계정 1회 $19) ②OV 클라우드 서명 구매(연 $100~400) ③자가서명(테스트 전용). → 서명 확정 전 설치형 배포 채널로 **클래식 설치기(Inno Setup)** 채택(PKG-5).
- **구성**: `scripts/nexa-setup.iss`(사용자 단위 기본 `PrivilegesRequired=lowest`+관리자 선택 허용 · 시작 메뉴/바탕화면(선택) · 언인스톨러[사용자 데이터 보존] · `AppId` 고정=업그레이드 식별자 · 라이선스 표시) + `scripts/make-setup.ps1`(포터블과 동일 self-contained 게시, **마커 없음=설치형 표준 AppData 경로** · ISCC 탐색 PATH→Program Files (x86)→사용자 설치).
- **CI**: `package` job에 setup 단계 추가(Inno=windows 러너 기본 포함, 로컬 설치 불요 — 사용자 지시) · 아티팩트 `packages-win-x64`(dist 전체) · 태그 시 릴리스에 **zip+setup.exe 모두 자동 첨부**.
- 로컬에 임시 설치했던 Inno Setup 6.7.3은 제거(사용자 지시 — CI 전용 운영).
