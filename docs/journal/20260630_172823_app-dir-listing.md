# 작업 기록 — 2026-06-30 17:28:23 (KST)

> 기록 ID: `20260630_172823_app-dir-listing`
> 이전 기록: `20260630_165817_interop-dir-enum`

## 1. 요구
- 다음 단위: 인터롭 디렉터리 열거(F3)를 **C#에서 호출해 UI에 표시** — 폴더 내용을 화면으로 확인.

## 2. 구현
- **C# 바인딩** [NativeInterop.cs](../app/Nexa.App/NativeInterop.cs):
  - `[StructLayout(Sequential)] NexaEntry`(name=IntPtr, kind, size, modified) — C ABI 미러.
  - `nexa_dir_open`([MarshalAs LPUTF8Str] string) / `nexa_dir_next`(ref) / `nexa_dir_close`.
  - `ReadDir(path)` 래퍼: open→next 반복→close, `name`은 `Marshal.PtrToStringUTF8`로 **즉시 복사**(네이티브 포인터 수명 차단).
  - `DirItem` DTO(+ `KindLabel`/`SizeLabel` 표시용), `NexaFileKind` enum(0/1/2).
- **UI** [MainWindow.xaml](../app/Nexa.App/MainWindow.xaml): ListView(`x:Bind` DataTemplate) + 헤더. [.xaml.cs](../app/Nexa.App/MainWindow.xaml.cs) `LoadDirectory()` 시작 시 사용자 홈 열거.
- **csproj**: `AllowUnsafeBlocks=true`(WinUI WinRT 제네릭 컬렉션 ABI 힌트 대응).

## 3. 검증
- `dotnet build app/Nexa.App -c Debug` → **0 warning / 0 error**(AllowUnsafeBlocks로 WinRT 힌트 해소, x:Bind 컴파일 OK).
- Nexa.App.exe 기동 → 예외 없이 실행 유지(인터롭 왕복 + 디렉터리 열거 동작). 검증 후 종료(dll 잠금 해제).
- (UI 육안: 홈 폴더 항목 목록이 종류/이름/크기로 표시 — `dotnet run`으로 확인 가능, docs/19 §F4.)

## 4. 메모
- IDE 힌트 `LibraryImportAttribute` 권장(4건)은 빌드 무관 → DllImport 일관 유지(추후 일괄 전환 검토).
- `DirItem`은 `record`(불변) — 다음 가상화/관찰 시 `ObservableCollection`/ItemsRepeater로 전환.

## 5. 다음
- WinUI **ItemsRepeater 가상화 렌더**(대량 항목 일정 메모리) → 경로 입력·네비게이션 → 인라인 트리(docs/07).
