# 작업 기록 — 2026-07-01 00:09:06 (KST)

> 기록 ID: `20260701_000906_macos-netsdk1100`
> 이전 기록: `20260630_235959_macos-debug-and-vm`

## 1. 요구
- 맥에서 `dotnet build app/Nexa.App` 실행 시 **NETSDK1100**(EnableWindowsTargeting) 오류 발생.
- 관련 **전체 문서 최신화**.

## 2. 사실 정리 (2단계 실패)
- 1단계: 옵션 없이 → `NETSDK1100`(Windows 타깃이라 맥에서 차단).
- 2단계: `-p:EnableWindowsTargeting=true` 우회 시 → `XamlCompiler.exe`(Windows 전용) `exit 126`.
- 결론: **WinUI 앱은 맥에서 빌드·실행 불가** → Windows/VM/CI. 맥은 코어(Rust)만. 우회 시도 비권장.

## 3. 갱신 문서 (지침 문서 일괄, journal은 과거 기록이라 미수정)
- `11 §6-1`: NETSDK1100 → XamlCompiler **2단계 실패** 정확 기술 + "맥 빌드 금지/우회 금지" 결론.
- `11 §4-3`: 빌드·실행 블록에 ⚠️ Windows 전용·맥 NETSDK1100 경고.
- `18 §2`: WinUI 빌드 섹션 상단에 ⚠️ 맥 실행 금지·NETSDK1100·VM 안내.
- `19`: 검증 명령 상단에 ⚠️ 앱 검증은 Windows/VM, 맥은 cargo test.
- `app/README.md`: 맥 빌드 금지 경고 + VM 링크.

## 4. (옵션) 후속 제안
- `app/Nexa.App.csproj`에 비Windows 빌드 시 **친절한 Error 타겟**(NETSDK1100 대신 "Windows에서만 빌드 — docs/11 §4-4") 추가 가능. 코드 변경이라 이번엔 문서만.

## 5. 다음
- 단위 백로그(15 §7) 계속 — 맥=코어/디버그, 앱=VM.
