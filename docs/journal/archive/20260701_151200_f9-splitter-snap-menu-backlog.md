# 작업 기록 — 2026-07-01 15:12 (KST)

> 기록 ID: `20260701_151200_f9-splitter-snap-menu-backlog`
> 이전 기록: `20260701_150618_a-track-resize-fix-sort-design`

## 1. 요청
- "메뉴/툴바는 스타일과 크기 두 관점이므로 정리. 스플리터를 개선하고 commit."

## 2. 정리 (메뉴/툴바 = 스타일 vs 크기)
- **크기**: ✅ 확정("적당") — 메뉴 28px·툴바 현 패딩 유지.
- **스타일**: ⏳ 후속(트랙 D) — 메뉴 항목 확장 + 아이콘 툴바(placeholder 제거).
- docs/19에 백로그 노트로 분리 기록.

## 3. 구현 (F9 스플리터 개선)
- **두께 축소**: `PanelSplitter`/`TermSplitter`/`BottomSplitter` 8→4px + 옅은 배경(`#22FFFFFF`)으로 가시화.
- **자석식 스냅**(좌/우 분리선, 양방향):
  - ① **창 중앙(50:50)** ② **상↔하 분리선 위치 정렬**. 임계 24px 내 흡착, 벗어나면 해제.
  - 분리선 위치 = 좌 열 `ActualWidth`. 좌 패널/하단 좌 도킹 Border `SizeChanged`에서 감지 →
    `SnapTarget`(중앙 우선, 없으면 상대 분리선) → `ApplySnap`(star 비율 재작성, `_snapping` 재진입 가드).
  - 듀얼/분리 상태에서만 동작(단일 패널·하단 미분리 시 스킵).

## 4. 검증
- `dotnet build app/Nexa.App -c Debug` → **0/0**.
- 육안: 스플리터 얇음, 중앙/상하 분리선 부근 드래그 시 흡착.

## 5. 다음 (백로그)
- **설정 시스템(JSON)**: 스플리터 위치·토글·컬럼(폴더별) 저장/복원 — 별도 설계 문서 예정.
- **A5 정렬 구현**, **메뉴/툴바 스타일**(트랙 D).
