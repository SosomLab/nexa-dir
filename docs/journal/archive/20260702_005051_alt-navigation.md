# 작업 기록 · Alt 방향키 네비게이션 (F21)

> 요구(최우선, 별도 커밋): Alt+↑=위로(상위), Alt+←/→=이전/다음(뒤로/앞으로).

## 구현
- OnGridKeyDown의 IsAltDown() 분기: ↑→GoUp, ←→GoBack, →→GoForward, ↓→ActivateItem(F19).
  GoUp/GoBack/GoForward(F13) 재사용 → 탭별 이동 기록(F20)과 자동 결합.
- docs/19 F21, docs/26 §5-4 단축키 표(Alt+↑/←/→ = nav.up/back/forward) 추가.

## 검증
- WinUI라 맥 빌드 불가 → push 후 CI(windows) app job green 확인.
- Windows 수동: 진입 후 Alt+←/→ 이전/다음, Alt+↑ 상위.
