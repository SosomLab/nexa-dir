# 작업 기록 — 2026-07-02 00:29 (KST) · 단축키 커스터마이즈 할 일 등록

> 기록 ID: `20260702_002952_shortcut-customization-todo`
> 이전 기록: `20260702_002427_alt-down-activate`

## 1. 요구
- 지금 하드코딩으로 설정하는 단축키들을 **나중에 사용자가 변경할 수 있는 설정**으로 구현 → 할 일로 등록.

## 2. 반영 (설계/백로그, 코드 변경 없음)
- 이미 `docs/26`(명령 레지스트리 + `keybindings.json` 재정의, FR-I2)이 설계돼 있음 → 여기에:
  - **§5-4 "현재 하드코딩 단축키 → 재정의 대상"** 표 신설(↑↓·Shift/Ctrl 범위·Space·→/←·← 부모·**Alt+↓**·탭·메뉴 + 명령 id 예정).
  - §7-β·§8에 **하드코딩 키 → 레지스트리/keybindings.json 이관** 할 일 명시.
- `docs/04` 백로그: "단축키 커스터마이즈(재정의)" roadmap 승격, "명령 팔레트"도 roadmap.

## 3. 계획(요지)
- 동작 코드 → 명령 `Execute`로 이동, `OnGridKeyDown`은 **Keymap 조회→명령 실행**으로 축소.
- 기본 키맵(defaults) + `keybindings.json` 덮어쓰기. 순수 로직은 `net8.0`로 분리해 맥 단위테스트.
- 시점: M1 레지스트리 토대 → P1 설정 시스템(β)에서 재정의.
