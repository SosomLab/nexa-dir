# 2026-07-01 · 작업 기록 (시간 역순)

> 일자 단위 상세 기록(아카이브 통합). 최신이 위. 상위 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md). 원본: [archive/](archive/).

## 23:57:49 · ←로 상위 폴더 이동 (F17-1) · 원본 `archive/20260701_235749_left-key-parent-nav.md`
- 요구: 인라인 트리에서 `←` 표준 트리 동작 — 펼쳐진 폴더는 접기, 접힌 폴더/파일은 상위(부모) 행 이동, 최상위(Depth 0)면 무동작.
- 헬퍼 `ParentIndex(list, index)`(현재보다 Depth 1 작은 최근접 상위, 없으면 -1). 이동 시 단일 선택+캐럿+BringIntoView. `→`는 기존 폴더 펼침 유지.
- 검증: 맥 빌드 불가 → push 후 CI(windows) app job green 필수.

## 23:47:55 · 진행 현황 업데이트 + 구현 분석 · 원본 `archive/20260701_234755_progress-review-analysis.md`
- 맥 최신화(Windows 진척 pull) 후 STATUS §6 F1~F17 확장, 코어 `cargo test --workspace` 9 green(맥 실측).
- 잘된 점: 인터롭 경계 견고(핸들 스트리밍·오류 격리), 컴포넌트화(`NexaFileGrid`·`NexaMenuBar`·`ColumnResizeGrip`), Explorer급 UX, 폴더우선 정렬 옵션화.
- 부채: ① 플래그십 트리가 앱(C#) 초안(코어 `VisibleRow` 스트림 C1 미구현, NFR-P1/P2 우려), ② `MainWindow.xaml.cs` 비대(721줄, 패널 분기 38회) → `Nexa.ViewModels`+`Panel` 필요, ③ 앱 테스트 0, ④ 러버밴드·C3/C4·파일작업·설정 JSON·경로 바 미구현.
- 권장 순서: (1) ViewModels+Panel 분리, (2) 코어 VisibleRow 스트림(C1), (3) 파일 작업 초안.

## 21:18:43 · 키보드 Ctrl+↑↓ 캐럿 · Space/Ctrl+Space 선택 · 원본 `archive/20260701_211843_keyboard-ctrl-space-select.md`
- 요구: Ctrl+↑/↓=캐럿만 이동, Space=단일 선택, Ctrl+Space=비연속 다중 토글(Explorer식).
- `DirItem.IsCaret` 추가 → 선택 안 돼도 얇은 포커스 외곽선(선택>캐럿>없음 우선). `MoveCaret` 헬퍼. `OnGridKeyDown`에서 Ctrl+↑/↓ 위치만 이동(early return). `UpdateSelectionCount` 공용화. 빌드 0/0.

## 21:10:07 · 키보드 Shift 범위 선택 + →/← 펼침·접힘 · 원본 `archive/20260701_211007_keyboard-range-expand.md`
- 요구: Shift+↑/↓ 범위 선택, → 확장, ← 접힘.
- 캐럿 분리: `_leftCaret`/`_rightCaret`(위치)를 `_leftAnchor`/`_rightAnchor`(범위 고정점)와 분리 → Shift 연속 확장. `OnGridKeyDown` 재작성(세로=캐럿 이동/Shift면 anchor~caret 범위, 가로=폴더 →펼침/←접힘). `OnToggleExpand`를 `SetExpanded(item, expand)`로 추출(클릭·키보드 공용). 빌드 0/0.

## 20:51:49 · UX 수정 — 리사이즈 커서·키보드·탭 클릭 · 원본 `archive/20260701_205149_ux-fixes-cursor-keyboard-tab.md`
- 커서: WinUI 3는 `ProtectedCursor`만 → `ColumnResizeGrip`(Grid 서브클래스) 신설, 생성자에서 `InputSystemCursor.Create(SizeWestEast)`.
- 키보드: 내부 ScrollViewer가 방향키 선점 → `AddHandler(KeyDownEvent, OnGridKeyDown, handledEventsToo:true)` 재배선. 탭 바 Border `Tag`+`OnTabBarTapped`→`SetActivePanel`+Focus. 빌드 0/0.

## 20:37:00 · 커맨드 팔레트 설계 + JSON 설정 연동 · 원본 `archive/20260701_203700_command-palette-design.md`
- 산출 docs/26 신설: 명령 레지스트리(SSOT, 메뉴·툴바·단축키·컨텍스트·런처·플러그인·팔레트 공유), 팔레트 UX(`Ctrl+Shift+P`, 접두 `>`/`/`/`@`/`?`, 퍼지·MRU), 키맵(`keybindings.json`).
- JSON 연동(§5): `settings.json`/`keybindings.json`/`commands.user.json`/`state.json` 4파일 양방향, `AppSettings`→`SettingsStore`, schemaVersion. 순수 로직 맥 테스트. 문서 전용.

## 20:31:25 · 폴더 우선 정렬 옵션화 (F15 초안) · 원본 `archive/20260701_203125_foldersfirst-sort-option.md`
- 이미 `ReadDir`에 하드코딩된 폴더 우선을 설정 값으로 분리. `Settings.cs` 신설(`SortOptions{FoldersFirst=true}`+`AppSettings.Sort` 인메모리, JSON 자리 주석). `ReadDir(...,SortOptions?)`+`SortItems` 헬퍼(초기 로드·펼침 동일 적용). 빌드 0/0.

## 19:47:26 · 네비게이션·선택·커스텀 메뉴 UX · 원본 `archive/20260701_194726_nav-selection-menu-ux.md`
- `NexaMenuBar`(289c911·5045e03·57147f0·87cefbd): 기본 MenuBar 한계 → Button 헤더+경량 Popup(사각 Border), hover 전환·ALT·ESC·바깥 클릭.
- 네비: 패널별 뒤로/앞으로 스택+현재 경로(`Nav`), 위로 버튼, 아이콘+더블클릭(25bb330, `DirItem.IconImage` 셸 썸네일 비동기+글리프 폴백).
- 선택/호버: 호버 옅은 파랑, 활성 파란 테두리, 비활성 회색, 테두리 항상 1px(높이 점프 방지), `SetActivePanel`+`_windowActive`. 클릭 지연(~300ms) → 선택을 `PointerPressed`(즉시)로. 키보드 ↑/↓ 단일 이동+`BringIndexIntoView`. 빌드 0/0.

## 16:15:00 · 파일 클릭 선택 (F11, 플래그십 C3 초안) · 원본 `archive/20260701_161500_file-selection-c3-draft.md`
- `DirItem.IsSelected`+`RowBackground`(선택 반투명 파랑). `OnRowTapped` 수정자별(단일/Ctrl 토글/Shift 범위/Ctrl+Shift 추가). 패널별 기준점(`_leftAnchor`/`_rightAnchor`), 로드 시 초기화. 수정자키 `GetKeyStateForCurrentThread`. 상태바 "N개 선택됨". 디스클로저는 `e.Handled`로 선택 분리. 빌드 0/0. 다음: 러버밴드·Ctrl+A·C4.

## 15:55:00 · 인라인 트리 펼침 + Finder 컬럼 (F10) + 스플리터 · 원본 `archive/20260701_155500_inline-tree-finder-columns.md`
- 설계(docs/23 §6): 컬럼 설정 모달·헤더 드래그 재정렬(`ColumnReordered`)·`NexaGridColumn.Visible` 예정.
- 구현: `NexaFileGrid.HeaderBackground` DP(기본 `#808488`). 스플리터 4px+`Foreground=Transparent`, 스냅 임계 20px, Alt 드래그 스냅 해제, 최소 160/80.
- 인라인 펼침(F10): `DirItem` 트리 모델(Depth/FullPath/IsExpanded), `ReadDir(path, depth)` 폴더 우선, 이름·수정한 날짜·종류·크기 컬럼, 이름 셀 들여쓰기+디스클로저(▶/▼)+폴더 아이콘, `OnToggleExpand`. 빌드 0/0.

## 15:12:00 · F9 스플리터 개선 + 메뉴/툴바 정리 · 원본 `archive/20260701_151200_f9-splitter-snap-menu-backlog.md`
- 크기 확정(메뉴 28px·툴바), 스타일 후속(트랙 D). F9: 스플리터 8→4px+옅은 배경(`#22FFFFFF`), 자석 스냅(중앙 50:50 / 상↔하 분리선 정렬, 임계 24px, `SizeChanged`→`SnapTarget`→`ApplySnap`, `_snapping` 가드, 듀얼/분리 시만). 빌드 0/0.

## 15:06:18 · 컬럼 리사이즈 수정 + 정렬 설계 · 원본 `archive/20260701_150618_a-track-resize-fix-sort-design.md`
- A3 `ManipulationDelta` 미동작 → `PointerPressed/Moved/Released`+`CapturePointer`(핸들 8px)로 교체. 헤더 색(`#18FFFFFF`+`#33FFFFFF`). 정렬 상태를 컬럼→패널(NexaFileGrid)별(너비만 공유).
- 설계(docs/23): §3-1 너비=실시간 동기·정렬=패널별 독립·폴더별 저장/복원. §4 헤더 클릭 정렬(▲/▼)+다중 컬럼(Shift/Ctrl)+초기화+`SortRequested`. 빌드 0/0.

## 12:58:28 · 컬럼 리사이즈+좌/우 동기화 (A3+A4) · 원본 `archive/20260701_125828_a3-a4-column-resize-sync.md`
- 공유 컬럼 방식: `NexaGridColumn.Width`를 INotifyPropertyChanged로, 하나의 인스턴스를 헤더·본문·좌/우 공유. `RootGrid.Resources`(ColKind/ColName/ColSize) 후 코드에서 `Columns.Add(Resources[key])`.
- 빌드 교훈: ① WinUI 3 `Window`은 `Resources` 미지원→`Grid.Resources`. ② `IList`에 `<StaticResource>` 추가 불가→코드비하인드. 빌드 0/0.

## 12:03:01 · 컬럼 헤더 + 컬럼 모델 (A2 초안) · 원본 `archive/20260701_120301_a2-column-header.md`
- `NexaGridColumn`(Header/Width/Key). `NexaFileGrid` 2행 Grid(헤더 Auto+본문 *), 헤더는 `Columns`를 가로 `ItemsRepeater`. 앱: 좌/우 컬럼(종류64/이름240/크기100), 본문 행을 고정 너비 StackPanel로 정렬. 한계: 헤더-본문 수동 정렬→A3에서 공유 너비. 빌드 0/0.

## 11:50:59 · NexaFileGrid 개명 + Nexa.Controls 제품화 · 원본 `archive/20260701_115059_nexa-controls-product.md`
- `VirtualizedTreeGrid`→`NexaFileGrid` 개명(`4472ff8`), `Nexa.Controls` 3rd-party 판매 가능 구성. 라이선스=PolyForm Noncommercial(DR-5). `Nexa.Controls.csproj` 패키지 메타(`PackageId=SosomLab.Nexa.Controls`, LICENSE 동봉). ADR-0002 §9-1(독립 제품·퍼미시브+WinUI만·코어 비참조). 빌드 0/0.

## 11:24:09 · VirtualizedTreeGrid 추출 (A1) · 원본 `archive/20260701_112409_a1-virtualized-tree-grid.md`
- A 트랙 세분화(A1 추출·A2 헤더·A3 리사이즈·A4 동기화·A5 정렬). `app/Nexa.Controls`(WinUI 클래스 라이브러리) `VirtualizedTreeGrid`(ScrollViewer+ItemsRepeater 래핑, `ItemsSource`/`ItemTemplate` DP). 앱 좌/우를 `<ctrls:VirtualizedTreeGrid>`로 교체.
- 빌드 교훈: ① MSB4062→`EnableMsixTooling`. ② MSB3073(리소스 DataTemplate+x:Bind+DP)→인라인 `x:Bind`. ③ exe 잠금→`Stop-Process`. 빌드 0/0.

## 11:09:15 · 우 패널 파일 목록 + 하단 도킹 연동 · 원본 `archive/20260701_110915_dual-panel-list-and-dock-link.md`
- 우 패널 placeholder→좌와 동일 구조(`DirRepeater2`/`DirHeader2`/`PathText2`), `LoadDirectory` 파라미터화. `UpdateBottomDock()`(하단 우=우 패널 표시 AND 하단 분리 ON, 숨김 시 "분리" 토글 비활성). 메뉴 `Height=28 Top`.
- 미구현(컬럼 동기화): 헤더/리사이즈 부재 → 선행 후 docs/23 §3. 빌드 0/0.

## 10:56:52 · TFM 불일치 수정 (Sizers) · 원본 `archive/20260701_105652_fix-sizers-tfm.md`
- 증상: Windows 첫 빌드 4 errors(`CS0234 CommunityToolkit.WinUI` / XamlCompiler exit 1). 원인: TFM 불일치(앱 19041 ↔ Sizers 제공 18362/22621). `Visible(bool?)`가 `Window.Visible`와 충돌.
- 조치: TFM `19041.0`→`22621.0`(min 17763 유지), 헬퍼 `Visible`→`Vis`. 빌드 0/0. 교훈: 앱 TFM은 참조 CommunityToolkit 제공 TFM과 정합.

## 02:07:41 · 레이아웃 골격 변경 내역 (F6) · 원본 `archive/20260701_020741_layout-change-record.md`
- 레이아웃 골격 3커밋(`105d9e8`→`7b982b9`→`597e52e`, +258/-38). 7행 그리드(메뉴·툴바·런처·좌/우 듀얼·하단 도킹·상태바), `ctk:GridSplitter` 크기 조절, 상태바 토글. 좌/우 50:50, 하단 좌/우 분리 기본. 의존성 `CommunityToolkit.WinUI.Controls.Sizers`(MIT)→NOTICES.

## 00:09:06 · 맥 NETSDK1100 결론 + 문서 최신화 · 원본 `archive/20260701_000906_macos-netsdk1100.md`
- 2단계 실패: 옵션 없이 NETSDK1100, `EnableWindowsTargeting=true` 우회 시 `XamlCompiler.exe` exit 126. 결론: WinUI 앱 맥 빌드·실행 불가(Windows/VM/CI). 문서 갱신(11 §6-1/§4-3·18 §2·19·app/README).
