# 2026-07-10 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).
> **⏱ 표기 = 해당 단위의 git 커밋 시각 범위(KST, 초 단위 원천=git log)** — 실제 착수는 첫 커밋 이전(직전 단위 종료 직후).

## 재시작 필요 설정 — 확인창 + 자체 재시작 (PREF-9, `feat/pref-9-restart` 브랜치)

> ⏱ 2026-07-10 (`8d7d4e4`) · 요청 2건: ① 할 일 등록(TODO §9-5) ② 구현 + **재시작 진행 과정·메커니즘 문서화**

- **요구(사용자)**: 언어 변경 등 재시작이 필요한 설정을 바꾸면 확인창을 띄우고 앱이 직접 다시 시작. 진행 과정과 메커니즘을 문서에 함께 기록.
- **동작**: 설정 창 언어 변경 → 저장 예약(기존 Changed 경로 그대로) → `Loc.IsPendingCultureChange`가 **실효 언어 변화**를 판정(설정값을 `Resolve`로 실코드화해 `Localizer.Current.Culture`와 비교 — ko→en→ko 원복이나 ""(시스템)↔"ko"(시스템=한국어) 등가 전환은 안 물음) → `ContentDialog`(지금 재시작[기본]/나중에). "나중에"도 저장은 유지되어 다음 시작 때 적용(기존 restartNote 동작 보존).
- **재시작 경로**: `MainWindow.RestartApp` = **선-flush**(settings.json+session.json 동기 저장 — `AppInstance.Restart`가 `Closed`/`ProcessExit` 훅 실행을 보장하지 않아 필수) → `AppRestart.Restart` = 1차 WinAppSDK `AppInstance.Restart`(패키지/미패키지 공용, 성공 시 미복귀) → 실패·예외 시 2차 폴백 `Process.Start(Environment.ProcessPath)`+`Application.Exit`(미패키지·포터블 안전망, 새 인스턴스 기동 실패 시 현 인스턴스 유지).
- **일반화**: 재시작 필요 설정은 항목별로 `MaybePromptRestart(판정 델리게이트)` 호출로 선언(현재 언어 1종, 후속 후보=테마팩). i18n `restart.*` 4키(en/ko `.lang`+임베디드 en).
- **문서**: 시퀀스·판정 규칙·구성 요소·설계 근거(왜 폴백/왜 선-flush) = [docs/40 §9](../40-preferences-system.md). 로컬 빌드 0w/0e.

## 4차 감사(refactor/004-audit) — bad smell 제거·핫패스 최적화·상주 메모리 (`refactor/004-audit` 브랜치)

> ⏱ 2026-07-10 (커밋 4개: `d17cf6c`·`bcd2fc8`·`a9c5682`·`4d05298`) · 병렬 분석 4종(C# 스멜/Rust/메모리/속도) 기반

- **분석**: 코드 전체를 4개 관점(C# bad smell 30건·Rust 12건·메모리 11건·속도 13건)으로 병렬 감사 후 SAFE/저위험부터 적용.
- **Rust 코어(`d17cf6c`)**: `cmp_ci` 비교마다 String 2개 할당 → 소문자화 이터레이터 직접 비교(O(n log n) 정렬 핫패스 무할당) · `find_prefix` 이름 전체 소문자화 → 접두사 길이만 비교(키 입력당 할당 제로) · **FFI 가드**: `add_sel` 범위 밖 id 거부(호스트發 잘못된 id가 `selected_paths` 인덱싱 패닉→extern "C" unwind/abort 하던 위험 차단) · `rebuild_visible` clone→`mem::take` · `visible_index` 사설 별칭 제거 · 인터롭 경량화(`visible_id`/`selected_path` 신설 — row 실체화 클론 제거, 선택 N순회 O(N²)→O(N)). cargo test 33 green.
- **C# 중복/수명(`bcd2fc8`)**: 클립보드 보일러플레이트 4곳→`TrySetClipboardText` · 외부 드롭 deferral 3곳→`ReceiveExternalDropAsync` · 플라이아웃 위치 2곳→`ShowFlyoutAt` · TerminalView.Stop이 선택 자동스크롤 타이머 미정지+세션 이벤트 미해제 → 정지/해제 · FolderWatcher 2개 Closed에서 Dispose · `ScheduleShellRefresh` 호출마다 타이머 생성→단일 재사용 · 체크섬 다이얼로그 ShowAsync try/catch.
- **핫패스/메모리(`a9c5682`)**: `VtScreen.Lines`(매 접근 전체 List 실체화, ~30fps)→`LineCount`/`LineAt` · RenderScreen run별 StringBuilder→프레임당 1개 · 세션 리셋 시 브러시 캐시 정리(트루컬러 증식) · `VirtualTreeCollection.Count` P/Invoke→캐시 · **행 재활용 시 실체화 캐시 이빅션**(방문 행 무한 보존→실화면 수준: 10만 폴더 스크롤 시 수십 MB + 선택 동기 P/Invoke 비례 증가 해소) · **DATAS 활성**(`runtimeconfig.template.json` — .NET8 힙 적응, 상주 규율 NFR 1차).
- **시작 경로(`4d05298`)**: settings.json을 OnLaunched·MainWindow가 각각 로드하던 중복 제거(1회 로드·적용).
- **미적용(후속 문서화)**: MainWindow 장대 메서드 분해(OnGridKeyDown 236줄 등 5곳) · 터미널 렌더 요소 재활용(전면 재구축→풀링, L) · 하단 도크 갱신 디바운스 · NexaRow에 경로 동봉(ABI v8) · 유휴 시 working set trim — 방안 보고서 참조.

## 앱 아이콘 신규 디자인 — 풀블리드 폴더 + 터미널 >_ (main 직접)

> ⏱ 2026-07-10 19:11:56 (`9150329`) · 실제 착수는 PR#12 병합(18:31) 직후

- **요구(사용자)**: 다크 테마용 아이콘 추가 요청에서 출발 — 색반전판 → 신규 컨셉 3후보(터미널 폴더/듀얼 패널/코드 블록) → 레퍼런스 이미지 2종 반영을 거쳐 **"풀블리드 폴더 + 대형 초록 `>_`" 마테리얼 플랫**로 확정. 최종 지시: **기본(nexa-dir) 세트 자체를 다크판으로 교체**.
- **디자인**: 캔버스를 꽉 채우는 폴더(뒤판 탭+앞판 2톤, 배경 투명=실루엣이 곧 아이콘) 위에 초록 프롬프트 `>_`(내장 터미널·개발자 지향). 앞판을 터미널 화면처럼 어둡게(#2A3344→#1C222E) 해 초록 대비 극대화(QA 라운드). **실루엣 테두리=accent 파랑 #3D8BFF** — 흰/검정 배경 합성 검증(240/96/32px)으로 양쪽 대비 확인(QA 라운드).
- **세트**: `nexa-dir`(다크판, 앱 창·작업표시줄 기본) + `nexa-dir-light`(밝은 폴더+진한 초록 #17B04A — 라이트 테마 전환용 예비, 미배선).
- **산출 슬림화(사용자)**: 실사용처가 ICO뿐(csproj ApplicationIcon+Content, 창 3곳 SetIcon)임을 확인 — 16~512 개별 PNG 제거, **1024 마스터(재생성용)+ICO만 유지**.
- `make-app-icon.ps1` 팔레트 주입식(`-Theme light|dark|all`)으로 전면 개편. 빌드 0w/0e·앱 재실행 확인.

## 타입어헤드 마감 — 전역 배선(활성 패널)·검색어 HUD (`feat/typeahead-hud` 브랜치)

> ⏱ 2026-07-10 17:05:07 (`a7c2002`) · docs/32 단계 3 + 배선 개선

- **요구(사용자)**: 타입어헤드 입력란이 안 보이고 빠른 찾기도 동작하지 않음 — 상태 점검 후 구현.
- **원인(무동작)**: 배선이 그리드 `CharacterReceived`(포커스 의존) — 경로 바/도구 버튼 클릭 등으로 그리드가 포커스를 잃으면 무반응(방향키는 전역 후킹이라 되는 것과 비대칭). → **RootGrid 전역 버블 수신 → 활성 패널 기준**으로 재배선(터미널/TextBox 소유 시 미개입 = `IsKeyboardOwnedByInput`).
- **검색어 HUD(§7-A 단계 3)**: `Nexa.Controls.EphemeralOverlay` 신규 — 도메인 비종속 휘발성 배지(검색 아이콘+텍스트, Show/Clear/Timeout 자동 소거, 히트테스트 불가). 각 패널 목록 좌하단 배치, **매치 실패에도 버퍼 유지 표시**(오타 인지, §7 결정 5), 소거=`TypeAheadTimeoutMs`(버퍼 리셋 동기).
- docs/32 상태 갱신: 단계 1~5 전부 ✅(잔여=§9 후속 — 초성·IME 미리보기·소리·UIA).
- **포커스 회수 수정(`5bbd47e`, QA)**: 터미널이 한 번 포커스를 가지면 파일 영역 클릭 후에도 포커스가 남아 방향키/타입어헤드가 `IsKeyboardOwnedByInput` 가드에 전부 차단(위/아래=터미널 입력·문자 무반응) → 전역 press에서 패널 내 클릭이면 그 패널 그리드로 `Focus(Programmatic)` 명시 회수(이름변경 편집기 클릭 제외).
- **크기 표기 탐색기식(`fe70914`·`105add1`)**: 목록 컬럼=적응 단위(1MB 미만 올림 KB, 이상 유효 3자리 MB/GB/TB) · 정보 패널 Size 줄=원시 바이트 "13,014 Bytes"(1이면 Byte, 단복수).
- **피커 hover 유지·절반 축소(`f36cf5a`)**: Button 템플릿 PointerOver가 hover 색을 덮어쓰던 문제 → 시각 상태 없는 Border 타일로 교체(벗어날 때까지 유지), 타일 32px·점 8px.
- **보조 창 아이콘(`99415e9`)**: 설정·전송 진행 창이 .NET 기본 아이콘으로 뜨던 것 → `AppWindow.SetIcon`(메인 창과 동일 ico) 적용.
- **HUD 위치 매트릭스 피커(`6369c64`→`36b6479`)**: 콤보 → 3×3 타일 그리드(머티리얼풍) — 각 타일이 미니 패널 내 배지 점 위치를 시각화, 클릭 즉시 반영, 툴팁=위치명. QA 라운드: 정사각 64px(1.5배)·점 14px 확대(`2c0afae`) · 입력 옵션 3종을 매트릭스 우측 배치 · **hover=강조색 50%/선택=100%/미선택=회색**(`36b6479`).
- **HUD 위치 3×3 + 입력 문자 옵션(`0d03f72`)**: HUD 표시 위치 설정(좌상/상/우상/좌/중앙/우/좌하/하/우하, 기본 좌하 — `HudPosition`+`ApplyHudPosition` 라이브) · 입력 문자 옵션 3종(특수문자 포함/Space 포함[접두사 진행 중]/Backspace 지우기 — 기본 전부 ON, 합성 게이트+영속+설정 체크박스).
- **파일명 문자 전체 확장 + Space 조건부(`efdd148`)**: 합성 맵을 파일명 허용 문자 전체(미국식 배열 — 영숫자·`- _ . , ; = + [ ] { } ~ \`` 및 Shift 기호·NumPad)로 확장, 금지 문자(\\ / : * ? " < > |) 제외. **Space=접두사 진행 중일 때만 포함**(평상시 선택 토글 유지 — §7 결정의 조건부 완화, 사용자 승인).
- **문자 트리거 KeyDown 합성 교체(`85f175a`, QA 검증)**: 화살표/수정키(전역 KeyDown)는 도달하는데 문자만 미도달 — **WinUI `CharacterReceived`는 포커스 요소에만 발화(비버블)** → RootGrid 전역 수신 불가로 확정. `OnGridKeyDown`(검증된 경로)에서 A–Z/0–9/NumPad/Backspace를 문자 합성해 `TypeAheadChar`(활성 패널) 위임. Ctrl/Alt 제외·Shift 허용, Backspace는 진행 중 접두사 있을 때만 소비. IME 한글은 후속(§9).
- **터미널 포커스 강탈 근본 수정(`1e2f96e`, QA — 진짜 원인)**: 선택 변경/파일 클릭 → `UpdateSelectionCount → RefreshBottomDocks → InfoText 갱신 → Render → EnsureTerminal → Start`가 **매번 `FocusSoon`으로 포커스를 훔침**(터미널 탭 활성 상태에서 ↑ 한 번 뒤 다음 키가 터미널 입력·파일 클릭이 터미널 클릭처럼·타입어헤드 무반응의 공통 뿌리). → `TerminalView.Start(focus)` 도입: 재렌더 경로(EnsureTerminal)는 포커스 미점유, 포커스는 사용자 의도 지점(Kind 전환·터미널 위치 이동)만.

## 패널·터미널 UX — 싱글 도크 대원칙·터미널에서 열기·선택 자동 스크롤 (`feat/panel-terminal-ux` 브랜치)

> ⏱ 2026-07-10 15:51:55 ~ 16:45 (`1b62a85`·`2644b2a`·`ca91d51`·`6293af4`·`51005b5`·`6d3beb3`·`ff53cdd`·`ece3501`) · PR #11

- **터미널 긴 출력(`51005b5`·`6d3beb3`·`ece3501`)**: ① ConPTY가 뷰포트 폭에서 하드랩 → PTY 폭 보장으로 줄바꿈 방지(가로 스크롤 열람) ② 오버레이 가로 스크롤바가 입력 줄을 가림 → 가로 익스텐트 초과 시에만 하단 16px 여백 예약 ③ **설정 "터미널" 카테고리 신설** — 긴 출력 처리(창 폭 줄바꿈 ↔ 최대 길이까지 줄바꿈 없음[기본]) + 최대 글자 수(80~1000, 기본 240) 입력, `TerminalOptions` 영속·라이브 반영 ④ 가로 스크롤바 클릭/드래그 시 텍스트 선택 동시 시작(버블링) 차단.
- **우 패널 분할 위치 저장·복원(`ff53cdd`)**: "항상 50:50" 결정 변경(사용자) — 숨김 직전 실측 비율 저장→재표시 복원(기본 0.5), 하단 도크와 동일 규칙.
- **테마 서브메뉴+기본 System(`ece3501`)**: NexaMenuBar **2단 서브메뉴 지원**(`NexaMenuEntry.Children`+SubPopup, hover/탭 펼침·▸) — 구성(O)의 테마 3항목을 "테마" 그룹으로. **테마 기본값 Light→System**(OS 추종, DR-2 주석 갱신).

- **하단 도크 분할 비율 저장·복원(`ca91d51`, QA)**: 우 도크 재표시 시 편중 잔존 → 숨김/분리 해제 직전 좌:우 실측 비율 저장, 재표시 때 그 비율(기본 0.5=50:50)로 좌/우 열 star 복원. (상단 파일 패널은 사용자 결정대로 항상 50:50.)
- **터미널 우클릭 복사(`6293af4`)**: 드래그 선택 상태에서 우클릭 = 선택 영역 클립보드 복사 후 해제(Windows Terminal 관례). 선택 없으면 무동작.

- **다크 테마 메뉴 바(`2644b2a`, QA)**: 드롭다운이 다크에서 안 보임(라이트 하드코딩 — 어두운 텍스트·라이트 팝업) → **테마 토큰 4종**(NexaMenuPopupBackground/Border·MenuText·MenuHover, Light/Dark)을 `ActualTheme` 기준 조회. 다크=크롬보다 조금 밝은 바탕(#262B33)+밝은 회색 텍스트(#CFD2D6), VS Code식. Popup 콘텐츠는 루트 RequestedTheme 미상속 → OpenMenu가 지정. 테마 전환 시 헤더 재구성. 토큰 없는 호스트=라이트 폴백(컨트롤 독립성).
- **우 패널 재표시 50:50(`2644b2a`, QA)**: 숨기기 전 스플리터 드래그로 좌 열 star가 편중되면 재표시가 50:50이 아니던 것 → 재표시 시 좌 열도 `1*`로 되써 항상 중앙 분할.

- **요구(사용자)**: ① 컨텍스트 메뉴 "터미널에서 열기 (내장)" — 폴더 1개 선택 시 그 패널 터미널 활성화+cd ② 터미널 선택 드래그 시 화면 밖(위/아래) 내용 선택 안 됨 — 가장자리 대기 시 자동 스크롤 ③ **하단 도크 대원칙**: 듀얼=좌↔좌·우↔우(현행 유지) / **싱글=활성 패널 탭 기준**(정보·터미널 이동 동일 기준), 듀얼 복원 시 싱글이던 도크=좌측 용·우측=정보 패널 재연결.
- **싱글 도크 대원칙**: `RefreshBottomDocks`가 도크 구성 인지 — 싱글이면 소스=활성 패널(정보·미리보기·`CurrentFolder` 모두). `SetActivePanel`(활성 전환)·`UpdateBottomDock`(싱글↔듀얼 전환)에서 재연결.
- **터미널에서 열기(내장)**: 레지스트리 항목 `open-in-terminal`(order 70, 폴더 1개 선택 시만) → `OpenInEmbeddedTerminal(left, folder)` 공용 경로(도구 모음·도크 이동 버튼 리팩터 합류) — 하단 패널·터미널 탭 자동 표시, 싱글 도크면 공용 도크.
- **터미널 선택 자동 스크롤**: 선택 드래그 중 뷰포트 상/하단 24px 대기 → 60ms 틱·40px 스크롤 + 마지막 포인터 위치로 `HitCell` 재계산(새 오프셋 기준 선택 끝 확장). 릴리스/선택 종료 시 타이머 정지.
- **검증**: 빌드 0 warning/0 error. 실기 QA 대기.

## 도구·메뉴 QA 확장 — 메뉴 정렬·터미널 cd 버튼·탭/파일명 복사·라벨 i18n (`feat/toolbar-groups`)

> ⏱ 2026-07-10 14:44 ~ 15:30 (`530be01`·`7ab39ae`·`12f2207`·`5f26711`·`3b4224f`·`4b5d482`·`c304097`·`2d1b12a`·`39ad313`) · PR #10

- **Copy as filename 위치(`12f2207`)**: 사용자 요청으로 Copy as path **바로 위**에 — `VerbReplacement.Before`(동사 대체 위치에 동반 삽입, 역순 InsertMenu로 나열 순서 유지) + `CmItemDef.InsertAboveVerb`. 대체 항목이 숨겨지면 고유 섹션 폴백. 탭 우클릭 복사 메뉴 명칭도 파일 컨텍스트와 통일(ctx.copyAs*, filename이 위).
- **명칭 통일(`5f26711`)**: filename/foldername → **"Copy as name"**(ko "이름 복사") 단일 키(ctx.copyAsName)로 파일 컨텍스트·탭 공용. 항목 id(copy-as-filename)는 설정 영속 키라 유지.
- **하단 도크 터미널 cd 버튼(`3b4224f`)**: 요청 위치 정정 — 정보란(정보/미리보기/Hex/터미널 토글 줄)의 터미널 토글에 밀착한 24×24 정사각 버튼. 이 도크 패널의 현재 탭 폴더로 터미널 cd(`TerminalCdTo` 재사용). 상단 도구 모음 버튼도 유지(양쪽 제공).
- **탭 전환 시 도크 폴더 미갱신 버그(`c304097`)**: 캐시 탭 전환(`SwitchToTab`)은 네비게이션 경로(`ShowCurrent`)를 거치지 않아 `RefreshBottomDocks` 미호출 → 터미널 위치 이동이 이전 탭 폴더로 가던 현상(꾸욱 누르면 선택 이벤트 경유 `UpdateSelectionCount→RefreshBottomDocks`로 우연히 갱신돼 동작하는 것처럼 보임 — 사용자 관찰로 특정). `SwitchToTab`에서 직접 갱신.
- **컨텍스트 메뉴 글꼴 슬롯(`4b5d482`, 검토 승인 후)**: `FontOptions.MenuFamily/MenuSize`(기본 Segoe UI 12) — `ApplyMenuFont`가 플랫 메뉴(탭·빈영역) 항목 전체(서브메뉴 재귀)에 표시 직전 적용(메뉴는 열 때마다 생성 → 다음 열림부터 반영). 설정 글꼴 카테고리 FontRow+영속+i18n 3벌. **셸(HMENU) 메뉴는 OS 글꼴이라 적용 불가** — 설명문에 명시.
- **도크 터미널+이동 버튼 한 몸 스타일(`2d1b12a`·`39ad313`)**: 토글 우측/버튼 좌측 라운딩 제거 + 간격 0(테두리 1px 겹침, Margin -3) — 분할 버튼처럼. **초록 강조(#3FA34D)는 터미널 활성 시에만**(그 외=테마 기본 버튼 색, `SyncToggles`에서 상태 기반). 회색 상태 클릭 = 터미널 전환+활성 탭 폴더 cd(기존 `TerminalCdTo`).

- **메뉴 바 드롭다운 정렬(`530be01`)**: 체크형 항목이 해제 상태일 때 체크 아이콘(Collapsed)이 사라져 텍스트가 왼쪽에 붙던 문제 → **체크 칸 18px을 전 항목 상시 예약**(체크불가/해제=투명)·패딩 단일화 — Icons/List와 체크 항목 텍스트 시작 위치 통일.
- **터미널 위치 이동 버튼(`7ab39ae`)**: 터미널 버튼에 밀착한 22×22 정사각(Attached, Spacing 상쇄) — 활성 패널의 활성 탭 폴더로 **내장 터미널 cd**(하단 패널·터미널 탭 자동 표시, cmd는 `/d`). 세션 미시작이면 시작 직후 전송(pending 입력).
- **탭 우클릭 복사(`7ab39ae`)**: [폴더 이름 복사 · 전체 폴더 경로 복사] 섹션(새 탭 아래) + 상태바 피드백.
- **Copy as filename(`7ab39ae`)**: Copy as path와 동일 대상 규칙(교차폴더 전체 선택·한 줄 하나)로 **리프 이름만** 복사. Copy as path/filename 라벨도 i18n(직접 구현 항목 — ko "경로 복사"/"파일명 복사", 구 PR#6의 셸 원명칭 유지 결정을 사용자 요청으로 대체).
- **검증**: 빌드 0 warning/0 error.

## 도구 모음 그룹화(파일 표시 토글)·순서 설정·플랫 컨텍스트 메뉴 (`feat/toolbar-groups` 브랜치, 3커밋)

> ⏱ 2026-07-10 14:27:39 ~ 14:33 (`9fad6bd`·`5d50169`·`52f2e63`) · docs/44 확장

- **요구(사용자)**: ① 도구모음에 "파일 표시" 그룹 신설 — 숨김 파일·(.) 파일 토글 2종(켜짐/꺼짐을 아이콘/토글로 표시), 기존 3버튼과 세로선 구분 ② 그룹/그룹 내 항목 순서를 설정 화면에서 직접 조정 ③ 우클릭 메뉴를 참조 이미지처럼 섹션 구분·토글 체크로 정리 ④ 탭 우클릭도 플랫하게. **(.) 파일 통용 명칭 = "도트파일(dotfiles)"로 추천·적용.**
- **A(`9fad6bd`) 그룹 레지스트리 + 토글**: `ToolbarGroupDef/ItemDef` 레지스트리(도구·파일 표시) 기반 `BuildToolbar` — 그룹 경계 세로 구분선. 숨김 토글=켜짐 RedEye/꺼짐 Hide **아이콘 교체**, 도트파일 토글=⋯ 글리프(꺼짐 흐림)+ToggleButton 강조. 표시(S) 메뉴와 동일 설정 공유 — `SetShowHidden/SetShowDotFiles` 단일 경로로 메뉴·도구모음·배경 메뉴 전부 동기. `ToolbarOptions`(그룹/항목 순서) 모델+`Toolbar` 섹션 영속.
- **B(`5d50169`) 순서 설정 UI**: 설정 "도구 모음" 카테고리 — 그룹(굵게)/항목(들여쓰기) 행마다 ▲/▼, 이동 즉시 도구모음 라이브 재구성+저장. 순서 규칙=나열 id 우선·미기재 id는 기본 순서 뒤(새 항목 안전).
- **C(`52f2e63`) 플랫 메뉴**: `NexaMenuPresenterStyle`(사각·패널 배경·1px 테두리) + `FlatMenu()` — 빈영역/탭 메뉴 적용. 빈영역 = [붙여넣기 Ctrl+V | 실행 취소·다시 실행 | 새로 만들기▶ | **숨김·도트파일 표시 ✓ 토글** | 새로고침 F5] 섹션 구성, 탭 = 기존 3섹션 + Ctrl+W 단축키 컬럼. `bg.refresh` 라벨 "(F5)" → 가속키 컬럼 이동.
- **검증**: 슬라이스별 빌드 0 warning/0 error · xUnit 81 green. 실기 QA 대기.

## 탭·경로를 "기본 글꼴"로 통합 — 경로 글꼴 슬롯 제거 (`feat/font-settings`)

> ⏱ 2026-07-10 13:10:57 (`6f6ba53`) · PR #9

- **요구(사용자)**: 탭의 크기와 경로 브레드크럼 폰트가 "기본 글꼴 크기"를 사용하도록(+편집 모드도 동일).
- **수정**: 경로 바·탭 제목이 기본 글꼴(Base)을 따르도록 통합 — **경로 글꼴 슬롯 제거**(6종→5종: 모델·영속·설정 UI·i18n 3벌). 탭엔 폰트 상속 루트(`ContentControl TabsFontHost`)를 추가해 제목 `FontSize=12` 하드코딩 제거, **탭 높이(MinItemHeight)는 기본 글꼴 크기에서 계산**(12px→20px 동일, 크기 변경 시 연동). 편집 모드는 `EnterEdit`의 컨트롤 폰트 복사로 자동 동일.

## 경로 바 편집 모드 높이·글꼴 불일치 수정 (`feat/font-settings`)

> ⏱ 2026-07-10 13:02:56 ~ 13:07 (`c6e1f67`·`c6e7b4d`) · PR #9

- **요구(사용자, 실기 QA)**: 브레드크럼 ↔ 경로 편집 모드 전환 시 ① 바 높이가 달라짐 ② 글꼴 크기도 다름.
- **높이(`c6e1f67`)**: 편집기(TextBox)의 자연 높이 > 브레드크럼 세그먼트 높이 — 편집 진입 시 브레드크럼을 접고 편집기를 보이므로 바 전체가 늘어남. `EnterEdit`에서 편집기 `Height`를 `PART_Scroll.ActualHeight`(브레드크럼 실측)로 고정 — 경로 글꼴 설정에 따라 변하므로 상수 대신 실측. 레이아웃 전(0)이면 가드.
- **글꼴(`c6e7b4d`)**: 브레드크럼 TextBlock은 컨트롤 폰트(경로 글꼴 설정)를 상속하지만 TextBox는 테마 기본(14px) 유지 → `EnterEdit`에서 컨트롤 `FontFamily/FontSize`를 편집기에 명시 복사.

## i18n 전면 적용 + 선택 테두리 방식 개선(-1px 겹침) (`feat/font-settings`)

> ⏱ 2026-07-10 12:09:24 ~ 12:56:57 (`96c5cfe`·`3819762`) · PR #9

- **요구(사용자, 실기 QA)**: ① 버튼·라벨·상태창 정보까지 i18n 미적용분 **전체 적용** ② 상단 테두리 제거로 선택 박스 top border가 안 보임 → **간격을 줄이는 방식**으로 박스 4면이 보이게.
- **테두리(`96c5cfe`)**: 선택 테두리 4면 1px 복원 + 행 템플릿 **상단 -1px 겹침**(Margin, Repeater 상단 1px 보정) — 인접 선택 행의 테두리가 같은 픽셀에 그려져 경계 1px·박스 온전·행 높이 균일. `EstimateRowStride` 동기(-1·폴백 19).
- **i18n(`3819762`)**: 신규 키 **~140종**을 ko/en `.lang`+임베디드 `en.json` 3벌에 추가하고 사용자 표면 전부 교체 — 컬럼 헤더·네비/경로바 툴팁·상태바(선택 수·작업 결과·오류)·행/빈영역/탭 컨텍스트 메뉴·완전삭제 확인·체크섬 대화상자·새로 만들기(기본 이름 포함)·도구/런처 툴팁·드래그 라이브 캡션·전송 진행/덮어쓰기 창·하단 도킹(토글·정보·미리보기)·종류(KindText)·터미널 시작 실패·undo/redo 설명. **ViewModels(FileOps/OperationHistory)·RecycleBin 예외 메시지도 `Localizer.Current.T`**(전송 "일부 실패" 표기에 연동). `NexaFileGrid` 빈영역 드롭 캡션은 호스트 주입형 문구로 분리(컨트롤은 로컬라이저 비의존). 잔여 비로컬라이즈 = 개발자 진단(ConPTY/ABI)·주석뿐.
- **검증**: 로컬 Debug 빌드 0 warning/0 error · xUnit 81 green.

## 다중 선택 테두리 2px 겹침 제거 (`feat/font-settings`)

> ⏱ 2026-07-10 12:03:51 (`8ed17a2`) · PR #9 — (후속 `96c5cfe`에서 -1px 겹침 방식으로 대체)

- **요구(사용자, 실기 QA)**: 다중 선택 시 인접 행의 위/아래 테두리가 겹쳐 2줄로 보임 — 상단 1px 제거.
- **수정**: 선택 테두리 `SelBorderThickness (1) → (1,0,1,1)` — 행 간격 0(밀도 라운드)에서 위 행의 아래 테두리 1px만 경계로 남음. 모든 행이 같은 두께(투명↔색)라 선택 시 높이 점프 없음. 캐럿 외곽선은 3면(좌/우/하)이 됨을 주석에 명시.

## 설정 창 QA 피드백 라운드 — 검색 위치·이중 필터·콤보 초깃값·폴더 굵게 (`feat/font-settings`)

> ⏱ 2026-07-10 11:56:31 (`2db69f3`) · PR #9

- **요구(사용자, 실기 QA)**: ① 검색은 상단 전폭이 아니라 **좌측 목록 최상단** ② 검색 시 좌측 트리를 그룹명 일치 **또는** 일치 항목 보유 그룹만으로 필터 + 우측은 **이름/설명/설정값=1 논리 그룹** 기준으로 제목·설명 일치 항목만 ③ 글꼴 입력란이 빈칸으로 보임(클릭해야 값 표시) ④ 폴더명 Bold 표시도 설정으로.
- **검색 UX**: 검색창을 좌측 컬럼 상단으로 이동. `CategoryVisible`(그룹명 일치 ∨ 일치 항목 보유 ∨ 일치 자식 보유)로 트리 재구성 + `EntryMatches`(제목 `LabelKey` ∨ 설명 `DescKey`)로 우측 필터. 그룹명 일치 그룹은 항목 전체 표시, 검색 중 그룹 클릭 = 필터 유지된 그 그룹만.
- **콤보 초깃값 버그**: WinUI 편집 가능 ComboBox는 **템플릿 로드 전 `Text` 설정이 무시**됨(빈칸으로 보이다 클릭 시 표시) → 목록 내 값이면 `SelectedItem` 즉시 선택 + `Loaded` 후 `Text` 재설정(쉼표 폴백 목록 대응) 2중 처리.
- **폴더 굵게 설정**: `FontOptions.FolderBold`(기본 ON=기존 표시 유지) — `DirItem.NameWeight`가 설정 참조, 변경은 패널 재로드 시 반영. 영속(`Fonts` 섹션)+글꼴 카테고리 체크박스+i18n 3벌.
- **검증**: 로컬 Debug 빌드 0 error/warning.

## 글꼴 설정 6종 + 밀도 + 설정 창 VS Code식 재구성 (`feat/font-settings` 브랜치, 3커밋)

> ⏱ 2026-07-10 09:17:08 ~ 09:47:18 (`432cf44`·`ae7ad0b`·`e078774`) · 설계 반영 [docs/40 §8](../40-preferences-system.md)

- **요구(사용자)**: ① 글꼴 슬롯 6종 — 기본(메뉴·설명·하단 정보창)/콘솔(터미널, 쉼표로 2개 이상)/경로(브레드크럼)/상태표시줄/파일 목록(헤더 포함)/파일 헤더(목록과 동일 글꼴·크기, 꾸미기만) ② 탭 높이 글자 보이는 최소 ③ 폴더/파일 행 상하 1px 여유만(Double Commander 수준 밀도) ④ 글꼴은 목록 선택+직접 입력, 적용/엔터/포커스 이탈 시 오류면 경고 ⑤ 설정 창을 구성요소별 좌측 트리 + 상단 설정 검색(VS Code 참조)으로 ⑥ 영속 설정 전부 분류 수록.
- **슬라이스 1(`432cf44`) — 글꼴 모델·영속·적용 배선**: `FontOptions`↔`FontSettings`(settings.json `Fonts`) + `Sort.FoldersFirst` 영속 신규. `MainWindow.ApplyFonts`가 영역 루트에 라이브 적용(메뉴바 `RefreshFonts`/경로바/상태바/파일그리드/하단 도킹). **App.xaml 암시적 TextBlock 스타일 제거** — Style이 상속값을 이겨 설정 폰트가 무시되는 문제의 근본 해소(영역 루트 컨트롤 FontFamily/FontSize 상속으로 전환). 터미널은 Consolas 상수 → `ApplyFont()`(셀 폭 '0' 20자 실측·격자 리사이즈·재렌더). 헤더 꾸미기 = `HeaderCell.TextWeight/TextStyle`(INPC)+`SetHeaderTextStyle`. 설치 글꼴 열거 = `InstalledFonts`(GDI `EnumFontFamiliesExW`, 무의존).
- **슬라이스 2(`ae7ad0b`) — 밀도**: 행 템플릿 Padding `0,3→0,1` + `StackLayout Spacing 2→0`(행 높이=아이콘 16+2=18px 수준) → `EstimateRowStride` 가산 제거·폴백 24→18 동기. 탭 `MinItemHeight 24→20`·행간 3→2·바 패딩 `4,3→4,2`. 컬럼 헤더 셀 `6,3→6,2`.
- **슬라이스 3(`e078774`) — 설정 창 재구성**: 상단 검색 + 좌 TreeView(모양[테마·글꼴]/파일 목록/탭/파일 작업/컨텍스트 메뉴/언어) + 우 편집기. **설정 레지스트리**(카테고리·라벨 키·빌더)로 카테고리 렌더와 검색이 동일 원천. 글꼴 UI = 편집 가능 콤보(목록+직접 입력, 엔터·포커스 이탈·선택 시 쉼표 토큰별 검증 → 미설치면 경고·미적용) + 크기 NumberBox(8~32). 영속 설정 전부 수록(타입어헤드 범위/리셋·상위 이동 정렬 위치·dwell 2종·탭 더블클릭 등) + dwell 타이머 라이브 재반영. i18n 키는 ko/en `.lang`+임베디드 `en.json` 3벌 동기.
- **검증**: 슬라이스별 로컬 Debug 빌드 0 error/warning · push 후 CI `app` job green 확인. 실기 QA(글꼴 변경·밀도 체감)는 사용자 확인 대기.
- **후속**: 테마팩·컬럼·단축키·런처·즐겨찾기 카테고리(docs/40 §5) · 기본 글꼴의 전역(탭 제목 등 비관리 영역) 확장 검토 · 언어 변경 시 설정 창 라벨 즉시 갱신.

## 도구 모음 정리 — 구형 자리표시자 버튼 제거 + 높이 최소화 (docs/44 후속)

> ⏱ 2026-07-10 08:48:20 (`7b42d39`)

- **요구(사용자)**: 신규 도구 3종(터미널 열기·파일 찾기·이름 바꾸기) 이전에 만들었던 도구 바 버튼과 관련 이벤트를 깨끗하게 제거 + 도구 바 Height 최소화.
- **제거**: `MainWindow.xaml` 도구 모음의 정적 자리표시자 8종(뒤로/앞으로/위로/새로고침 글리프 4 + 아이콘/목록/컬럼/갤러리 텍스트 4)과 `AppBarSeparator` 2개. 확인 결과 전부 `x:Name`·`Click` 없는 XAML 전용 자리표시자 — **코드비하인드에 지울 이벤트 없음**(경로 바의 `BackBtnL`/`FwdBtnL`/`UpBtnL`·마우스 XButton 네비는 별개 기능, 유지).
- **잔존 구성**: 도구 바 = `InitToolbars()`가 채우는 `ToolbarToolsHost`(내장 도구 3종)만. 뷰 전환(아이콘/목록/컬럼/갤러리)은 표시(S) 메뉴 항목으로만 남음(뷰 모드 구현 시 재설계).
- **높이 최소화**: 도구 바 `Border` 세로 패딩 `6,4`→`6,2` + `AddToolButton` 버튼에 `MinHeight/MinWidth=0`(WinUI 기본 Button `MinHeight=32`가 바 높이를 결정하던 것 해제 → 16px 글리프 + 상하 패딩 수준).
- **검증**: 로컬 Debug 빌드 0 error/warning · push 후 CI `app` job green 확인.
