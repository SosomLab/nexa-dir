# 작업 기록 — 2026-06-30 16:44:47 (KST)

> 기록 ID: `20260630_164447_winappruntime`
> 이전 기록: `20260630_161136_interop-poc`

## 1. 증상
- `dotnet run --project app/Nexa.App` 실행 시 대화상자:
  "This application requires the Windows App Runtime 1.6 (MSIX package version >= 6000.519.329.0)".

## 2. 원인
- unpackaged WinUI 앱은 **빌드**는 NuGet(Microsoft.WindowsAppSDK)으로 되지만, **실행**엔 시스템에 Windows App Runtime이 필요.
- 점검 결과: 1.6 **framework** 패키지(6000.519.329.0)는 있었으나 **Main/DDLM(Singleton) 구성요소가 1.6용은 없음**(1.8용만 존재).
- winget `Microsoft.WindowsAppRuntime.1.6`은 "이미 설치됨"으로 framework만 인식 → 부족.

## 3. 조치
- **공식 인스톨러**로 전체 세트 설치(Framework+Main+DDLM):
  `https://aka.ms/windowsappsdk/1.6/latest/windowsappruntimeinstall-x64.exe` → `--quiet` (exit 0).
- 설치 후 확인: `MicrosoftCorporationII.WinAppRuntime.Main.1.6` 6000.519.329.0, `WinAppRuntime.DDLM.6000.519.329.0`(x64/x86) 추가됨.
- **실행 검증**: 빌드된 `Nexa.App.exe` 기동 → 종료 없이 유지(런타임 정상 로드, 인터롭 PoC 표시). OK.

## 4. 재현성 반영
- `scripts/bootstrap.ps1`: Windows App Runtime 1.6 설치 단계 추가(Main.1.6 미설치 시 공식 인스톨러 다운로드·설치).
- `docs/18` §6-2: 앱 실행 전제로 런타임 설치 명시(winget framework만으론 부족, 인스톨러 권장). 배포 시 self-contained 대안 메모(docs/12).

## 5. 참고
- 이 PC에 `cargo-deny` 0.19.9도 설치(라이선스 게이트 로컬 실행용).
- 다음 단위: `feat(core/vfs)` 로컬 디렉터리 스트리밍 열거.
