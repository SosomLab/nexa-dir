# 2026-06-30 · 작업 기록 (시간 역순)

> 일자 단위 상세 기록(아카이브 통합). 최신이 위. 상위 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md). 원본: [archive/](archive/).
> 프로젝트 킥오프 → 설계·ADR·라이선스 확정 → 스캐폴딩 → M0 데이터 흐름(인터롭·열거·가상화) → 레이아웃까지 하루에.

## 23:59:59 · macOS Rust 디버그 검증 + Windows VM 가이드 · 원본 `archive/20260630_235959_macos-debug-and-vm.md`
- 맥 `cargo test --workspace` 9 green, `libnexa_interop.dylib` 빌드 OK; `rust-lldb`로 `nexa_poc_add` 브레이크포인트 적중 → 맥 Rust 디버그 정상. WinUI 앱은 맥 불가.
- `.vscode/launch.json`(F5 CodeLLDB) 추가, docs/18 §5-1/§6-2·docs/11 §4-4(Windows VM: Apple Silicon=Win11 ARM64 Parallels/UTM/VMware) 최신화.

## 18:25:43 · 컬럼 시스템 설계 · 원본 `archive/20260630_182543_column-system.md`
- 결정(docs/23): 컬럼=데이터 — `IColumn`+`ICellValueProvider`+`ColumnLayout`(직렬화). 기본열(VFS 메타)·수식열(샌드박스 화이트리스트 `=size/1024 & " KB"`)·플러그인열.
- 듀얼 동기화=독립/실시간/수동 적용, 동기 단위=`ColumnLayout`, 정렬은 셀 정렬키. 요구 FR-A8 추가.

## 18:21:51 · 재사용 컨트롤 라이브러리 · 원본 `archive/20260630_182151_reusable-controls-lib.md`
- 결정(ADR-0002 §9): 별도 WinUI 클래스 라이브러리 `app/Nexa.Controls`(예정). 도메인 비종속 `VirtualizedTreeGrid`(`IRowSource`/`IColumn`/`ISelectionModel`)+파일 어댑터. ItemsRepeater+Sizers만 참조→NuGet 패키지화·단위 테스트. docs/16 현행화.

## 18:15:12 · ADR-0003 뷰 모드·하단 패널 모듈 · 원본 `archive/20260630_181512_adr0003-modules.md`
- 결정(ADR-0003, docs/22): 뷰 모드=전략 `IFileView`(Details=FileTreeGrid·Icon·Column(Miller)·Gallery), 하단 패널=도킹 모듈 `IToolPanel`+`PanelHost`(Terminal=ConPTY). 둘 다 플러그인 확장점. 레이아웃 docs/20·FR-A4/A7.

## 18:04:58 · ADR-0002 커스텀 트리-그리드(FileTreeGrid) · 원본 `archive/20260630_180458_adr0002-fileview.md`
- 플래그십 4요건(트리·다중 컬럼·폴더 교차 선택·10만 가상화) 동시 충족 — ListView/TreeView/DataGrid 모두 불가.
- 결정(ADR-0002, docs/21): `ItemsRepeater` 기반 커스텀 `FileTreeGrid`. 코어가 가시 행(VisibleRow) 평면 스트림 산출→가상화가 평면 리스트로 환원. 교차선택=코어 `OrderedSet<NodeId>`, 셸 메뉴=`IContextMenu` 호스팅. 구현 단계 1✅→6.

## 17:57:14 · 레이아웃·네비게이션 요구 등록 · 원본 `archive/20260630_175714_layout-and-nav-requirements.md`
- 요구: 화면 레이아웃(영역/크기변경/숨김), 탭별 네비 히스토리(우클릭 N단계 점프), 메뉴 바+아이콘 도구모음(16/32px), 브레드크럼 우클릭 텍스트 주소 모드.
- 반영: docs/20 보강(다이어그램·표), FR-A2/A2b/A3/A6, 백로그 docs/15 §7. 방침: 요구 안정 후 골격 착수(문서 우선).

## 17:46:20 · WinUI ItemsRepeater 가상화 렌더 · 원본 `archive/20260630_174620_itemsrepeater.md`
- ListView→ScrollViewer+ItemsRepeater(StackLayout), 열거 로직·DTO 재사용(플래그십 인라인 트리·10만 노드 토대). 빌드 0/0. 후속: 인라인 트리 행·코어 VisibleRow·10만 벤치.

## 17:28:23 · 인터롭 디렉터리 열거 C# UI 표시 · 원본 `archive/20260630_172823_app-dir-listing.md`
- `NativeInterop.cs` `NexaEntry`(StructLayout)·`nexa_dir_open/next/close` 바인딩, `ReadDir` 래퍼(name은 `Marshal.PtrToStringUTF8` 즉시 복사), `DirItem` DTO. ListView(x:Bind) 홈 폴더 열거. `AllowUnsafeBlocks=true`. 빌드 0/0, 앱 기동.

## 16:58:17 · 인터롭 핸들 기반 디렉터리 열거 API · 원본 `archive/20260630_165817_interop-dir-enum.md`
- 배치 대신 핸들 스트리밍(open/next/close) 채택. Rust FFI `nexa_dir_open`/`nexa_dir_next`(1/0/-1)/`nexa_dir_close`, `#[repr(C)] NexaEntry{name,kind,size,modified_unix_ms}`(modified=SystemTime→i64 ms). 각 unsafe `# Safety`·널 검사. interop 테스트 4개, 9 green.

## 16:52:28 · nexa-vfs 로컬 스트리밍 열거 · 원본 `archive/20260630_165228_vfs-streaming.md`
- `Entry`에 size/modified 추가, `read_dir_entries(path)` 지연 Iterator(엔트리별 Result로 개별 실패 격리, 심볼릭링크 우선 판정). OS 비의존. vfs 테스트 3개, 7 green.

## 16:44:47 · Windows App Runtime 1.6 설치 문제 · 원본 `archive/20260630_164447_winappruntime.md`
- 증상: `dotnet run` 시 "Windows App Runtime 1.6 필요" 대화상자. 원인: unpackaged 실행엔 Main/DDLM 필요(winget은 framework만). 조치: 공식 인스톨러 `--quiet` 설치→기동 유지. `bootstrap.ps1`·docs/18 §6-2 반영.

## 16:11:36 · 인터롭 Rust↔C# 왕복 PoC · 원본 `archive/20260630_161136_interop-poc.md`
- M0 첫 단위: C ABI `nexa_poc_add`(c41dc41), C# P/Invoke(`nexa_abi_version`/`nexa_poc_add`, Cdecl), MainWindow 왕복(try/catch). csproj `BuildNexaInterop`/`CopyNexaInterop`(cargo→dll 복사), CI app job에 Rust 툴체인 추가. 실측 `abi=1`·`poc_add(2,3)=5`·`(40,2)=42`.

## 11:47:43 · Git 계정 분리 + 저장소 가시성 정정 · 원본 `archive/20260630_114743_git-identity-and-visibility.md`
- 레포 로컬 `user.name/email`=Sangyong Bae/kiros33@gmail.com(글로벌 미변경). `gh repo view`→PRIVATE 확인. 방침=현재 private, 진행 후 public(DR-5). 문서를 "공개 예정(현재 private)"으로 정정, 비밀 커밋 금지 규율 적용.

## 11:30:21 · 빌드/테스트 문서(docs/18) 신설 · 원본 `archive/20260630_113021_build-test-doc.md`
- `docs/18-build-and-test.md` 신설(코어 Rust/WinUI 앱/cargo-deny/CI/tools), 로컬=CI 일치. 갱신 규약을 문서 상단+CLAUDE.md §6에 명문화(신뢰 원천=CI).

## 10:22:35 · Windows PC 개발환경 부트스트랩·검증 · 원본 `archive/20260630_102235_devenv-bootstrap-verify.md`
- 이 Windows PC(KIROS33-GALAXYB) 환경 설치(`bootstrap.ps1`): Rust 1.96.0(+msvc), VS Build Tools 2022, .NET SDK 9.0.315(global.json 8.0 rollForward). 검증: `cargo test` 4 green, `dotnet build app/Nexa.App` 0/0 → 풀 빌드 가능. 부수: `bootstrap.ps1` SDK 감지를 `dotnet --list-sdks` 기준으로(런타임 오판 버그 수정).

## 02:17:38 · 진행 정리·STATUS 동기화 · 원본 `archive/20260630_021738_status-sync.md`
- STATUS.md(M7 추가·개발모델·스캐폴딩 완료·인덱스 16/17), docs/16, CLAUDE.md 현행화. 코어 green 재확인. 스냅샷: DR-1~5 확정, 스캐폴딩 완료, 로드맵 M0~M7.

## 02:07:31 · 패키지 관리자 정책 + 라이선스 정품 인증(M7) · 원본 `archive/20260630_020731_licensing-activation.md`
- R16 패키지 관리자(mac=brew, Win=choco→winget→수동)→`bootstrap.sh` 신규·`ps1` 개편·docs/11. R17 라이선스 정품 인증→docs/17 신설. 모델: Ed25519+PASETO v4.public 서명 토큰(앱엔 공개키만·오프라인 검증), 머신 지문 node-lock, 생성기=별도 private 툴, 온라인 2차=CF Workers. Rust `nexa-license`·`fingerprint`·`nexa-license-gen`. 로드맵 M7(L1~L10).

## 01:20:00 · 리포 스캐폴딩(M0 착수) · 원본 `archive/20260630_012000_scaffolding.md`
- 자동화: `.claude/settings.json` 커밋(dev 명령 allow, 파괴적=ask). 스캐폴딩(단위=커밋): ① Rust 워크스페이스(nexa-core/vfs/interop, deny.toml, cdylib+`nexa_abi_version`) ② LICENSE.md(PolyForm Noncommercial)+NOTICES ③ WinUI 앱 스켈레톤 ④ GitHub Actions(fmt/clippy/test·cargo-deny·WinUI 빌드) ⑤ 재현성(bootstrap.ps1/global.json/.vscode). 코어 맥 green.

## 01:09:00 · 개발 방법론 문서화 · 원본 `archive/20260630_010900_dev-methodology.md`
- R15: 작은 기능 단위 순차+단위별 커밋+초안→확장. `docs/15-dev-methodology.md` 신설(수직 슬라이스·항상 green·Conventional Commits·M0→M1 백로그). CLAUDE/README/STATUS 갱신.

## 01:06:31 · 컨텍스트 공유(CLAUDE.md·비밀 관리) · 원본 `archive/20260630_010631_context-sharing.md`
- R14: 다른 PC clone 시 컨텍스트 즉시 공유. 루트 `CLAUDE.md` 신설(이식용 메모리, 자동 로드), `docs/14`(공개=저장소/비공개=밖 채널), `.gitignore`(.env/*.pfx/*.pem/secrets/). `~/.claude` 로컬 메모리는 clone 미전달→CLAUDE.md 반영.

## 01:01:43 · 라이선스 확정(DR-5) · 원본 `archive/20260630_010143_licensing-confirmed.md`
- 확정: PolyForm Noncommercial 1.0.0(대안 BSL 1.1), 저장소 public 예정, 개인·비상업 무료/상업 유료. docs/13·10(DR-5+OD3 해소)·README·STATUS 신설. CLA·상표·퍼미시브 의존성+cargo-deny·NOTICE.

## 00:54:04 · 4대 핵심 결정 확정(DR-1~4) · 원본 `archive/20260630_005404_decisions-confirmed.md`
- DR-1 스택=Rust 코어+WinUI 3(ADR-0001 Accepted), DR-2 프로툴 고밀도(Path Finder풍), DR-3 MSIX+Releases+winget+포터블, DR-4 AI 보류(M5 전). R11 `docs/11` 신설(코어=맥/WinUI=Windows). R12 포터블 docs/12, R13 내장 zip FR-M(향후).

## 00:41:42 · 계층 경로 바 + 임베디드 터미널 · 원본 `archive/20260630_004142_pathbar-terminal.md`
- R9 상단 계층 경로 바→FR-A2 ★우선(M1 P0). R10 하단 임베디드 터미널→FR-K2b(설계 동결/구현 P2, ConPTY·코어 `pty` 이식 격리). D14~D16: 경로 바 P0, 터미널 설계만 동결, 외부 터미널(런처)과 임베디드 분리.

## 00:38:33 · 스크립트 플러그인 아키텍처 · 원본 `archive/20260630_003833_plugin-architecture.md`
- R8: `docs/09` 신설, FR-L(M6). 확장점: 명령·컨텍스트 메뉴·커스텀 컬럼·미리보기·VFS 공급자·자동화. 2-tier(T1 인프로세스 WASM/WIT·Lua + T2 아웃오브프로세스 RPC Python/Node), 매니페스트+contribution points, Capability 권한+서명, `.nexaext`. Rust `nexa-plugin`(wasmtime/mlua).

## 00:33:14 · 툴체인/배포 판단 기준 ADR · 원본 `archive/20260630_003314_tooling-criteria.md`
- R7: docs/06 평가 매트릭스 재구성(VSCode/CI 가중). (a) VSCode 단독 개발=A안 가능(Rust 완전·WinUI는 VS Build Tools CLI·최약점 C++). (b) Actions windows-latest→MSIX→signtool→Releases 자동화. 매트릭스: A 하이브리드 86(1위). OD1=성능 vs 툴체인.

## 00:29:03 · 상주 리소스 규율 NFR · 원본 `archive/20260630_002903_resource-discipline.md`
- R6: docs/05 NFR(M2 유휴 <150MB/트림 후 <80MB, M3 자니터, M4 압박 시 캐시 해제, N1 저우선 Background I/O, R3 오류 격리, R4 soak, R5 비차단), docs/01 §5-1. D10~D13: 하드 캡+LRU·백그라운드 자니터·작업집합 트림·위험 디코딩 격리 워커.

## 00:25:50 · 퀵 런처 바(FR-K) · 원본 `archive/20260630_002550_launcher-bar.md`
- R5: 타이틀바 하단 아이콘 런처(터미널·외부 에디터 실행 단축)→FR-K(M1 P1), docs/03 §3-K(인자 토큰), 로드맵 M1. 질의 "Rust Windows syscall이 C++ 대비 부족한가?"→ADR-0001 보강 예정.

## 00:21:42 · 요구사항 확장 · 원본 `archive/20260630_002142_requirements-expansion.md`
- docs/05·06(ADR-0001 Proposed)·07 신설. R1 ★플래그십(인라인 확장+교차 선택) P0(가시 노드 평면 스트림+가상화). R2 좌/우+탭(FR-B/C). R3 셸 컨텍스트 메뉴+고유 병합(FR-J). R4 클라우드 Provider trait 단계 확장(SMB+SFTP→S3→SaaS). OD1 스택 개발 전 확정.

## 00:12:53 · 프로젝트 킥오프 · 원본 `archive/20260630_001253_kickoff.md`
- 목표: 차세대 Windows 탐색기 `nexa-dir`(SosomLab/nexa-dir). 스택="성능 최대 네이티브"→하이브리드 Rust 코어+WinUI 3(ADR-0001). M1=탭+듀얼 우선. 설계 문서 세트(README·docs/00~04). D1~D4: 하이브리드·탭/듀얼·VFS 통일·가상화(첫 렌더 <150ms). 미해결 OD1~O4.
