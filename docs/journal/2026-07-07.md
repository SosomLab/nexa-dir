# 2026-07-07 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).

## B-2 셸 컨텍스트 메뉴 — ADR-0005 + S1 구현 (행 우클릭 = 클래식 셸 메뉴 + 고유 병합)

- **[ADR-0005](../38-adr-0005-shell-context-menu.md) Accepted**(`095cbfe`): 우클릭 = **클래식 네이티브 셸 메뉴(`IContextMenu` HMENU) + 고유 항목 병합**(사용자 확정 — 클래식 스타일 선호). ID 대역 분리: 셸 1~0x7FFF / 고유 0x8000+. Win11 신형 메뉴는 호스팅 공개 API 없음. in-proc 확장 크래시 리스크 수용(탐색기 동급). docs/10 ADR 색인 갱신.
- **S1 구현**: 신규 `ShellContextMenu.cs`(수동 COM 인터롭 — ShellLink/OleDropTarget 패턴):
  - `SHParseDisplayName`→`SHBindToParent`→`IShellFolder.GetUIObjectOf(IContextMenu)` → `QueryContextMenu`(HMENU) → 고유 항목 `AppendMenu`(구분자+0x8000+) → `TrackPopupMenuEx(TPM_RETURNCMD)` → 대역 분기(셸=`InvokeCommand`(CMINVOKECOMMANDINFOEX, UNICODE|PTINVOKE) / 고유=콜백).
  - **동적 서브메뉴**("보내기"/"열기 방법")·owner-draw: `IContextMenu2/3` QI + **메뉴 표시 구간만 `SetWindowSubclass`**(comctl32)로 WM_INITMENUPOPUP/DRAWITEM/MEASUREITEM/MENUCHAR 포워딩.
  - Shift 우클릭 = **CMF_EXTENDEDVERBS**(확장 동사). 셸 확장 예외는 try/catch 격리(메뉴 실패≠앱 크래시).
- `OnRowContextRequested` 교체: 기존 XAML 행 메뉴 → 셸 메뉴. 고유 병합 항목 = **폴더에 붙여넣기**(폴더+클립보드)·**이름 바꾸기(F2)**(인라인 편집기 — 셸 rename 동사는 호스트 밖 무동작이라 CMF_CANRENAME 미사용)·**완전 삭제(Shift+Del)**. 교차 부모 선택은 클릭 항목 폴더 기준 축소(S3 확장 예정). 셸 명령 실행 후 **800ms 지연 양쪽 재로드**(셸 동사 비동기 — watcher 보완).
- 빈영역 XAML 메뉴(붙여넣기·새로만들기·새로고침)는 유지 — **S2**(배경 셸 메뉴)에서 대체 예정.
- 빌드 경고 0·에러 0.

## BUG-009 해결 — 상승(관리자) 프로세스 OLE 드롭 폴백 (`OleDropTarget.cs`, [TODO B-16dnd](../TODO.md) ☑)

- **원인 확정**: 이 PC는 **UAC OFF(`EnableLUA=0`) → 탐색기에서 실행해도 모든 프로세스가 상승(High IL)**(진단 로그 `startup elevated=True` 실증). WinUI 3의 XAML 드롭 수신(WinRT 드래그 스택)은 **IL 일치 여부와 무관하게 상승 프로세스의 인바운드 드래그를 거부**(플랫폼 제한, microsoft-ui-xaml#7690/#10119 — #10119가 `EnableLUA=0` 정확히 같은 재현). 창 루트 DragEnter 프로브 무기록으로 XAML 미도달 확인. 어제의 "일반 권한 재현 → UIPI 배제" 판단은 무효(UAC OFF에선 일반 권한 실행이 존재하지 않음).
- **해결**: 상승 감지 시 XAML 콘텐츠 브리지 HWND(`DesktopChildSiteBridge`)에 **고전 Win32 OLE `RegisterDragDrop` + 자체 `IDropTarget`**(신규 `OleDropTarget.cs`, ShellLink.cs식 COM 인터롭) 등록 — 같은 IL 간 고전 OLE는 차단되지 않음(Double Commander `uOleDragDrop.pas` 동일 방식으로 방증). `CF_HDROP` 경로 추출, 원본 허용 효과와 교집합, 화면좌표→XAML DIP 역변환+`FindElementsInHostCoordinates` 히트테스트(폴더 행 우선, 아니면 포인터 아래 패널 현재 폴더). 연산 규칙·전송 엔진은 XAML 경로와 동일(`DragOp`/복사 기본·Shift=이동/`TransferPathsInto`), 이동은 최적화 이동(앱 수행 → 원본에 None 보고). **비상승은 미등록**(XAML 정상 경로 유지). 상승 시 내부(패널 간) 드래그도 함께 복구.
- **검증**: 탐색기→앱 파일 복사 실기 확인(사용자). 빌드 경고 0·에러 0. 진단 임시 코드(DndLog·루트 프로브·DebugDnd) 제거.
- **잔여 한계**: UAC ON에서 명시적 관리자 실행(Medium→High 교차 IL)은 고전 OLE도 차단 — 탐색기 동급 제한. 필요 시 Files식 안내 후속. 상세 [BUGS.md](../BUGS.md) BUG-009 · [docs/33](../33-file-ops-dnd-design.md) 함정 절 갱신.

## BUG-009 등록 — 외부(탐색기→앱) 드래그 금지 커서 (→ 위 항목에서 해결)

- DND-EXT 구현 후에도 탐색기→앱 드래그가 금지 표시. **UIPI 가설 배제**: 관리자 셸 실행이 원인 맞았던 1차와 달리, 탐색기에서 직접 실행(일반 권한)해도 재현.
- 의심: **unpackaged**(`WindowsPackageType=None`) WinUI 3에서 외부 드래그의 `DataView.Contains(StorageItems)` 미노출 또는 DragOver 미도달(WinAppSDK 이슈 계열).
- **진단 로그 심음**(임시, `ExternalDragOp`→`DebugDnd`): 외부 드래그 DragOver 도달 시 `%TEMP%\nexa-dnd-debug.log`에 `AvailableFormats`·StorageItems 여부 기록(0.5s 스로틀). 다음 세션: 탐색기에서 목록 위 드래그 → 로그 유무로 원인 분기(기록 있음=포맷 판정 수정 / 없음=unpackaged·권한 조사). 상세 [BUGS.md](../BUGS.md) BUG-009.
- 부수 확정: **UIPI 테스트 함정**(관리자 실행 시 DnD 차단)은 [docs/33](../33-file-ops-dnd-design.md)에 기록(`ee82a73`).

## Drag & Drop 전면 검토 → P1/P2/P3 순차 개선 (3커밋)

WinUI 기준 전면 검토([docs/33](../33-file-ops-dnd-design.md) "2026-07-07 후속 개선" 절) 후 우선순위대로 구현:

### 슬라이스 3 (P3, `e171a08`)
- 캡션(행·탭·본문) **변경 시만 설정** — DragOver는 마우스 이동마다 오므로 동일 값 재설정 회피(비교-설정, 상태 캐시 없음).
- DropCompleted None 문구 "드래그 취소 — 변경 없음".

### 슬라이스 2 (P2, `b8f8dc8`)
- **드래그 시작 병렬화**: StorageItem 취득(항목당 브로커 왕복) 순차 → `Task.WhenAll`. 대량 선택 시작 지연 완화.
- **외부 이동 후 원본 갱신**: DropCompleted에서 Move + `_dragPaths` 잔존(=외부 대상[탐색기]이 이동 수행)이면 `ReloadBothPanels()`.

### 슬라이스 1 (P1, `09ec2b2`)
- **DND-EXT 외부 드롭 수신**: 폴더 행·탭·빈 영역이 외부 파일 드래그(StorageItems)를 수락 — Drop에서 deferral+`GetStorageItemsAsync` → 기존 `TransferPathsInto` 합류. 복사 기본(Shift=이동). 텍스트 드래그는 금지.
  - 버그였던 것: 빈 영역이 외부 드래그에 "이동" 캡션으로 **수락 표시 후 무동작**(내부 `_dragPaths` 의존), 행/탭은 금지 커서.
- **DND-CYCLE**: 자기 자신/하위 폴더로의 드롭을 `DragOp`에서 None — UI 단계 차단(기존: 전송 단계 FileOps 예외 → "일부 실패").
- 그리드 API: `BodyDropped`/`BodyDragOperation`이 `DragEventArgs` 전달(DataView/deferral 접근) + 죽은 폴백 제거.

검증: 각 슬라이스 빌드 green(경고 0·에러 0). 외부 드롭·순환 금지·대량 드래그는 실행 앱에서 수동 확인.

## 내장 터미널 UX 일괄 개선 — 캐럿(BUG-007)·포커스·키 매핑·faint 예측(BUG-008 부분)·고정폭 렌더·ECH

한 세션에서 실사용 시나리오(입력→예측→백스페이스)를 따라가며 문제를 연쇄 해결. 각 단위:

### 7) Backspace 키 매핑 — 1글자 삭제로
- **증상**: 백스페이스 1번에 입력 전체가 지워짐.
- **원인**: `\b`(0x08=^H)를 전송 → ConPTY가 **Ctrl+Backspace(단어 삭제, BackwardKillWord)** 로 해석.
- **해결**: `OnKeyDown`에서 `VirtualKey.Back` → **DEL(0x7F)**(1글자), **Ctrl+Backspace → 0x08**(단어 삭제) — 실제 터미널과 동일 매핑. `OnCharacterReceived`의 `\b` 중복 처리 제거.

### 6) ECH(CSI n X)·커서 저장/복원 — 백스페이스 잔상 제거
- **증상**: `dot` 입력 후 백스페이스 → 캐럿은 이동하나 글자·이전 예측이 화면에 잔존.
- **원인**: ConPTY가 라인 일부 지우기에 쓰는 **ECH(`CSI n X`)** 미구현(EL/ED만 지원).
- **해결**: `VtScreen`에 ECH 구현(커서 위치부터 n칸 지움, 커서 유지) + **DECSC/DECRC(ESC 7/8)·CSI s/u** 커서 저장/복원 실제 구현(기존 무시).

### 5) 고정폭 셀 렌더 — 캐럿·열 정렬 (+ 가운데 정렬 회귀 수정)
- **증상**: 캐럿 위치가 글자와 어긋남(특히 `➜` 글리프·한글 이후 누적 드리프트).
- **원인**: 라인을 가로 StackPanel에 run 이어붙이기 → 폴백 폰트 글리프 폭이 고정폭과 달라 열이 누적 밀림. 한글(전각)을 버퍼에서 1칸으로 취급해 셸(2칸 계산)과 불일치.
- **해결**: 라인을 **Canvas 절대 배치**(run을 열×CharW에 고정)로 교체 — ASCII run은 묶고, 비ASCII는 글자별 자기 열 고정. `VtScreen.Put`에 **전각 2칸 전진**(wcwidth 근사 `IsWide` + 연속 셀 `'\0'`). 배경 Border도 셀폭 고정.
- **회귀**: 명시 Width+기본 Stretch 정렬이 **가운데 배치**로 동작 → `HorizontalAlignment.Left` 고정으로 수정.

### 4) faint(SGR 2) 렌더 — PSReadLine 예측을 연한 회색으로 (BUG-008 부분 해결)
- **진단**: 내장 터미널은 `pwsh`(프로파일 로드)를 구동 — `$PROFILE`의 oh-my-posh(robbyrussell)+PSReadLine 확인. "과거 history 보임"의 정체 = **PSReadLine 인라인 예측**(faint로 출력)인데 faint 미반영이라 실제 입력처럼 진하게 보였던 것.
- **해결**: `TermCell.Faint` 추가(셀 보관·run 분리 포함) → 렌더에서 **Opacity 0.45** — VS Code처럼 연한 회색 미리보기. 미사용 `_priv` 정리(경고 0).

### 3) 클릭 포커스 안정화
- **증상**: 터미널 클릭 시 포커스가 잡혔다 풀림(마우스 release 시점).
- **원인**: 동기 `Focus()`가 WinUI 기본 포인터/포커스 처리에 덮임 + 내부 ScrollViewer가 이벤트를 Handled 처리하면 XAML 속성 핸들러 미호출 + **release 기본 처리가 포커스 재탈취**.
- **해결**: `PointerPressed`/`PointerReleased`를 **`AddHandler(handledEventsToo:true)`** 로 등록하고 `FocusSoon()`(dispatcher enqueue — 기본 처리 이후 재포커스).

### 2) Tab 자동완성 — 포커스 이동 차단
- **증상**: 터미널에서 Tab이 자동완성 대신 다른 컴포넌트로 포커스 이동.
- **해결**: `OnKeyDown`에 Tab 추가(`Handled=true`로 WinUI 포커스 내비 차단) — Tab=`\t`, **Shift+Tab=`ESC[Z`**(역방향 완성). `OnCharacterReceived`의 `\t` 중복 제거.

### 1) 캐럿(커서) 표시 — BUG-007 해결
- `VtScreen`에 커서 좌표 노출(`CursorCol`/`CursorRow`/`ScrollbackCount`).
- `TerminalView`에 **캐럿 오버레이**(CaretLayer Canvas) — **포커스 시 반투명 accent 블록+깜빡임(530ms)**, **비포커스 시 외곽선(중공)** = 포커스 여부 시각화. 출력 직후 즉시 표시. `GotFocus`/`LostFocus`로 전환.

검증: 각 단위마다 로컬 빌드(`dotnet build app/Nexa.App`) green + 앱 실행 수동 확인(이 PC는 WinUI 로컬 실행 가능). 최종 **경고 0·에러 0**.
