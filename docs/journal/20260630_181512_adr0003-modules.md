# 작업 기록 — 2026-06-30 18:15:12 (KST)

> 기록 ID: `20260630_181512_adr0003-modules`
> 이전 기록: `20260630_180458_adr0002-fileview`

## 1. 요청 (사용자)
- **추상화 계층(Abstract/Interface)** 으로 구현 로직을 쉽게 교체·확장·모듈화.
- **뷰 모드**: 아이콘 / 목록 / 계층(컬럼·Mac식) / 갤러리.
- **하단 패널 모듈**: 상세정보(단일/다중)·미리보기·Hex·터미널(Cmd/Pwsh) — 배치/위치/크기 선택.

## 2. 결정 (ADR-0003, [22](22-adr-0003-view-and-panel-modules.md))
- **뷰 모드 = 전략(`IFileView`)**: 코어 데이터(FileSource/Selection) 공유, 뷰는 교체.
  - DetailsView(=FileTreeGrid, [21](21-adr-0002-fileview-control.md)) · IconView · ColumnView(Miller) · GalleryView.
- **하단 패널 = 도킹 모듈(`IToolPanel` + `PanelHost`)**: DetailsPanel(단일 메타/다중 목록·합계)·PreviewPanel·HexPanel·TerminalPanel(ConPTY Cmd/Pwsh).
  - 배치/위치/크기/표시 선택 + 세션 저장, 활성 선택 구독.
- 둘 다 **플러그인([09](09-plugin-architecture.md)) 확장점** — 인터페이스 구현만으로 추가, 코어 불변.

## 3. 추상화 계층 (핵심)
- 데이터(뷰 독립): Provider · FileSource(VisibleRow) · Selection(OrderedSet) · Sort/Filter.
- 뷰 전략: `IFileView`(Kind/Bind/스크롤/이벤트) + 4 구현.
- 도킹: `IToolPanel`(Dock/CreateView/OnContext) + `PanelHost`(배치/저장).

## 4. 문서 현행화
- `docs/22-adr-0003-...` 신설(맥락/결정/인터페이스/뷰·패널 정의/근거/영향/단계).
- 레이아웃 [20](20-ui-layout.md): 도구모음 뷰 전환(아이콘/목록/컬럼/갤러리), "하단 도킹 패널"로 일반화, §5 뷰모드&패널 섹션.
- 요구 [05](05-requirements.md): FR-A4(교체형 4뷰), FR-A7(하단 도킹 패널 모듈).
- 인덱스 STATUS/16에 doc 22.

## 5. 영향
- 코어(Rust): FileSource/Selection/Sort 안정 API. 뷰/패널이 구독.
- 앱(C#): `IFileView`(+4)·`IToolPanel`(+모듈)·`PaneHost`/`PanelHost`. FileTreeGrid=DetailsView.

## 6. 다음
- 인프라 우선: `IFileView`/`IToolPanel` 인터페이스 + Host 골격(추상화 먼저) → DetailsView 컬럼 헤더(ADR-0002 §7-2).
