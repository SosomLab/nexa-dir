# 2026-07-09 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).
> **⏱ 표기 = 해당 단위의 git 커밋 시각 범위(KST, 초 단위 원천=git log)** — 실제 착수는 첫 커밋 이전(직전 단위 종료 직후).

## 도구 모음(내장) + 퀵 런처(외부 프로그램) 슬라이스 1 (`feat/toolbar-launcher` 브랜치)

> ⏱ 2026-07-09 (브랜치 작업 — 병합 시 커밋 시각 확정) · 설계 [docs/44](../44-toolbar-and-launcher.md)

- **요구(사용자)**: 두 종류의 빠른 실행 도구를 16×16 아이콘으로 — **도구 모음**(개발자 제공 내장 기능)·**퀵 런처 바**(사용자 등록 외부 프로그램). 향후 단축키 배정 연결.
- **결정(3문)**: ① 터미널=**외부 터미널 실행**(내장 하단 아님) ② 파일 찾기=**스텁**(검색 M3 예정) ③ 이번 범위=**버튼·실행·아이콘·시드만**(CRUD/영속은 후속).
- **도구 모음 3종**([`MainWindow.InitToolbars`]): 현재 폴더 **터미널 열기**(`E756`, wt→pwsh→powershell→cmd 폴백·**Ctrl+Shift+T**) · **파일 찾기**(`E721`, M3 스텁) · **이름 바꾸기**(`E8AC`, F2 공용 `RenameCaret`).
- **퀵 런처 시드 1종**: **VS Code — 활성 탭 폴더 열기**(`Code.exe "%path%"`, `%path%`=활성 탭 폴더). **exe 아이콘 16px**(`GetThumbnailAsync(SingleItem,16)`, 실패 시 라벨 폴백). exe 경로 추정([`ToolLauncher.ResolveVsCode`]: 사용자/시스템 설치) — 미설치면 시드 제외.
- **실행 헬퍼**([`ToolLauncher`]): 터미널·외부 프로그램·VS Code 탐지. 실패 격리(미설치/권한=false → 상태바 안내).
- **UI**: `MainWindow.xaml` 툴바 행 `ToolbarToolsHost`·퀵 런처 행 `LauncherHost`(placeholder 제거, 코드에서 버튼 채움).
- **검증**: 로컬 Release 빌드 0 error/warning · 실행 확인(VS Code=`C:\Program Files\Microsoft VS Code\Code.exe` 탐지). CI green.
- **후속**: CRUD+영속(PREF-6 설정 "런처" 페이지) · 단축키 배정(PREF-5 keybindings) · `%selection%` 토큰 · 파일 찾기 M3 연결.
