# 작업 기록 — 2026-06-30 16:58:17 (KST)

> 기록 ID: `20260630_165817_interop-dir-enum`
> 이전 기록: `20260630_165228_vfs-streaming`

## 1. 요구
- 다음 단위: 인터롭으로 디렉터리 엔트리 스트림을 C#에 전달(핸들 기반 열거 API).

## 2. 설계 결정
- **핸들 기반 스트리밍**(open/next/close) 채택 — 배치 반환보다 설계 방향(핸들 API + 진짜 스트리밍, docs/01·07)에 부합.
- `NexaEntry.name` 수명: 핸들이 현재 엔트리의 `CString`을 보관 → **다음 next/close 전까지 유효**(엔트리별 별도 해제 불요).
- `modified`는 `SystemTime` → C ABI에 `modified_unix_ms: i64`(없으면 -1)로 변환.

## 3. 구현 (초안, Rust FFI)
- `nexa-interop` deps에 `nexa-vfs` 추가.
- API:
  - `nexa_dir_open(*const c_char) -> *mut DirHandle` (널/경로오류/IO 실패 시 널)
  - `nexa_dir_next(*mut DirHandle, *mut NexaEntry) -> c_int` (1=엔트리, 0=끝, -1=널인자; 엔트리 오류는 건너뜀)
  - `nexa_dir_close(*mut DirHandle)`
- `#[repr(C)] NexaEntry { name, kind(0/1/2), size, modified_unix_ms }`.
- 안전성: 각 `unsafe extern "C"` 함수에 `# Safety` 문서, raw 포인터 널 검사, 핸들로 Box 수명 관리.

## 4. 검증
- 테스트 4(interop): abi · poc · `dir_handle_enumerates`(임시폴더 open→next 루프→close, name/kind/size) · `dir_open_null_and_missing`.
- 게이트: fmt clean · clippy(-D warnings) clean(unsafe FFI 포함) · `cargo test --workspace` **9 tests green**.

## 5. 다음
- **C# 확장**: `NativeInterop`에 `nexa_dir_open/next/close` + `NexaEntry`(StructLayout) 마샬링 →
  디렉터리 열거 호출 → WinUI ItemsRepeater 가상화 렌더. (이때 dll 빌드 통합/실행은 기존 경로 재사용)
