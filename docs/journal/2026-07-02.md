# 2026-07-02 · 작업 기록 (시간 역순)

> 일자 단위 상세 기록(아카이브 통합). 최신이 위. 상위 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md). 원본: [archive/](archive/).

## 23:45:58 · refactor/002 통합 감사 (2차 라운드 시작) · 원본 `archive/20260702_234558_refactor-002-audit.md`
- 1차 방식 차용, 병렬 5축(성능·설계수용성·구조리팩토링·문서정합·**Double Commander 비교**) 감사를 `b38e6b3`(main, C1 병합 직후·태그 `0.1.0` 베이스라인) 기준으로 통합. 원칙: 1차 완료분 제외, 이월+신규만으로 2차 기준 수립.
- 총평: 설계 훼손 없고 C1(코어 트리 이관)으로 개선 — 진실원천이 C# `ObservableCollection+bool`→**코어 nexa-tree 가시행 스트림 + OrderedSet 선택**으로 단일화. 전면 아키텍처 변경 불필요(Q3 직답).
- 손볼 3대: ①성능 UI 병목 **P1**(열거·펼침 UI 스레드 동기→백그라운드), **P2**(펼침/접힘 Reset→`NexaRange` 범위 diff+캐시 시프트), **P3**(코어 경로→NodeId 조회로 O(m·n) 제거) ②구조(ViewModels 부재·`bool left` 이중화·XAML 복붙) ③설계 계약 공백(**nexa-ops·VFS Provider·watcher·에러 모델 표준**, M1 마감 전 동결).
- Double Commander 비교: WinUI 3/.NET UI는 유휴 메모리 2배±·배포 3–5배·콜드스타트 열위 — "UI가 네이티브 준함"은 사실 아님. ADR-0001 전면 재검토 트리거 아니며 **NFR-M2/P5 재보정+배포 이원화+상주규율**으로 대응.
- 2차 백로그 트랙 A(성능 A-1~A-5)·B(구조 B-1 ViewModels/B-2 PanelView/B-3 PanelControl)·C(설계계약 C-1~C-5)·D(배포·NFR 재보정)·E(퀵윈: 죽은 `nexa_dir_*` 제거·`panic="abort"`·`symlink_metadata`·문서 스테일·ADR-0004 docs/10 등재) 확정.

## 13:06:15 · refactor-001 통합 감사 (1차 라운드 시작) · 원본 `archive/20260702_130615_refactor-001-audit.md`
- 브랜치 `refactor/001-audit`(green HEAD `6e81734` 기반), **읽기 전용 진단**(코드 변경 없음). 소스+문서+진행 요약을 앱코드·설계정합·코어/빌드 3개 병렬 감사로 통합.
- 한 줄 결론: 지금 고치면 싼 것 = **① 코어 VisibleRow 스트림(C1) + ② `Nexa.ViewModels` 추출**. C3(교차선택)·C4(ops)·D(명령 레지스트리)를 `ObservableCollection<DirItem>`+bool 선택 위에 더 쌓기 전 처리 필요.
- A(최우선 구조): A1 플래그십 트리/선택이 `MainWindow.xaml.cs` 코드비하인드 축적·O(n)·NFR(10만/60fps) 위배 · A2 ViewModels 부재(로직 1124줄·맥 테스트 불가) · A3 ABI 버전 미검사·구조체 레이아웃 가드 없음·csbindgen 미사용.
- B(안전성): B1 FFI 패닉 언와인딩(`panic="abort"`) · B2 CI가 P/Invoke 왕복 미검증 · B3 attrs/심링크/스트리밍 무테스트 · B4 심링크 metadata follow 혼재 · B5 빌드 취약(하드코딩 경로·arm64) · B6 cargo-deny 게이트 미검증.
- C(리팩토링 본과제): C1 `bool left` 이중화 ~20메서드→`PanelView` 객체 · C2 "전체 해제 후 선택" 6+회 중복 · C3 좌/우 XAML ~90줄×2 복붙→`PanelControl` · C4 `OnGridKeyDown` 170줄 분해 · C5 색상/치수 산재→ResourceDictionary · C6 죽은 코드. D(문서): STATUS LOC 스테일·커밋해시 미기입·FR-A2 세그먼트 무동작·nexa-ops 부재.

## 11:52:42 · 마우스 네비게이션 + 단축키/창위치 설계 + F24 보완 · 원본 `archive/20260702_115242_mouse-nav-and-settings-design.md`
- **F25 마우스 뒤로/앞으로**: XButton1=뒤로/XButton2=앞으로 → 활성 패널(`_activeLeft`)의 GoBack/GoForward. RootGrid PointerPressed(handledEventsToo)에서 마우스만 처리.
- F24 보완(재요청): 두 토글을 동일 "보기" 개념으로 통일·**기본 표시(체크 ON)**. `HideDotFiles`→`ShowDotFiles`, 기본 true, "점(.) 파일 숨기기"→"점(.) 파일 보기".
- 메뉴 바 흰색: `#33FFA500`→`#FFFFFF`, 헤더 텍스트 어두운색(#1A1A1A)·활성 헤더 연한 파랑(#CCE4F7).
- 설계 반영(구현 대기): 단축키 시스템(docs/26) 입력 제스처 모델 §2-1 신설(키보드·마우스 버튼을 같은 `Binding`, 다중 바인딩) · **창 위치/세션 복원 docs/28 신설**(state.json 저장·복원, 다중 모니터 `isVisibleEnough` 보정→Primary LEFT/TOP) · 설정 화면 UI 별도 백로그.

## 10:41:11 · 숨김 파일 보기 · 점(.) 파일 보기 (F24) · 원본 `archive/20260702_104111_hidden-dotfile-toggles.md`
- 요구: 표시(S) 메뉴 독립 토글 2개. 숨김 판정 원천 **C# vs Rust 코어** 비교 → 성능·정확성·핫패스=코어(DR-1)로 **Rust 코어 확장** 재결정.
- 근거: Rust `DirEntry::metadata()`가 Windows에서 추가 syscall 없음(코어가 이미 크기·시각용 호출)→`attrs` 노출 비용 0. C# `File.GetAttributes`는 엔트리당 syscall 1회(10만=10만 회).
- 코어: vfs `Entry.attrs: u32`+`file_attrs()`(`#[cfg(windows)]`), interop `NexaEntry.attrs`, **ABI v1→v2**·테스트 `abi_version_is_two`.
- 앱: `ViewOptions{ShowHiddenFiles, ShowDotFiles}`, `DirItem.Attrs/IsHidden(0x2)/IsDotFile`·필터 `IsVisible`, `NexaMenuEntry` 체크형(글리프)·`OnToggleShowHidden/OnToggleShowDotFiles`→`ReloadBothPanels`. 탭 제목 폰트 9→12.
- 검증: 코어 `cargo test --workspace` 9 green · 앱 로컬 빌드 0 err · CI(windows) app job green 필수.

## 01:27:00 · NexaPathBar α — 계층 경로 바 (F23) · 원본 `archive/20260702_012700_nexapathbar-alpha.md`
- 요구/설계 docs/27. α = 브레드크럼 클릭 이동 + 우클릭 텍스트 편집.
- `Nexa.Controls/NexaPathBar`(UserControl): `Path` DP·`Navigated` 이벤트(네비 비종속). 브레드크럼=`ItemsRepeater`(세그먼트 클릭·드라이브 C:→C:\), 편집=TextBox(우클릭 진입·전체선택·복붙·Enter/Esc), `PathSegment` 모델.
- MainWindow: 좌/우 네비 바→NexaPathBar(PathBarL/R), `LoadDirectory`가 `pathBar.Path` 설정·`OnPathBarNavigated`(Directory.Exists→Navigate). docs/19 F23·05 FR-A2b.
- 후속(β/γ): 오버플로 …/UNC·형제 ▾ 드롭다운·드롭 타깃·VFS 스킴·템플릿드 컨트롤화.

## 01:09:15 · 탭 닫기(Ctrl+W·더블클릭) + 더블클릭 동작 설정 (F22) · 원본 `archive/20260702_010915_tab-close.md`
- 요구: Ctrl+W 탭 닫기 + "탭 더블클릭 시 동작" 설정(없음/닫기/즐겨찾기/팝업, 기본 닫기).
- Settings.cs: `TabDoubleClickAction`+`TabOptions.DoubleClick=Close`+`AppSettings.Tab`.
- MainWindow: `CloseTab`(최소 1개 유지·이웃 전환)·`OnTabDoubleTapped`(설정 분기)·`OnGridKeyDown` Ctrl+W. docs/19 F22·05 FR-B6a.

## 00:50:51 · Alt 방향키 네비게이션 (F21) · 원본 `archive/20260702_005051_alt-navigation.md`
- 요구(별도 커밋): Alt+↑=위로, Alt+←/→=뒤로/앞으로.
- `OnGridKeyDown` `IsAltDown()` 분기: ↑→GoUp, ←→GoBack, →→GoForward, ↓→ActivateItem(F19). GoUp/Back/Forward(F13) 재사용→탭별 이동 기록(F20)과 결합.
- docs/19 F21·docs/26 §5-4. CI(windows) app job green 확인.

## 00:45:20 · 패널별 탭 (F20) · 원본 `archive/20260702_004520_panel-tabs.md`
- 요구: 좌/우 패널 탭(멀티라인+고정크기), 탭 영역 더블클릭 추가, 활성 탭 상단 하이라이트.
- `PanelTab.cs` 신설: 탭별 경로·이동기록·펼침 상태 + Title/IsActive(INPC)·TabBackground·AccentVisibility.
- MainWindow: 패널별 `ObservableCollection<PanelTab>`+활성 탭, `_leftNav/_leftExpanded`→활성 탭 접근자, `SwitchToTab/AddTab/OnTabTapped/OnTabBarDoubleTapped`. XAML 탭 바=`ItemsRepeater`+`UniformGridLayout`(132px·말줄임)·활성 2px 하이라이트.
- docs/19 F20·05 FR-B4~B7. 후속: 배치/크기 설정·중간클릭·드래그·세션 복원.

## 00:29:52 · 단축키 커스터마이즈 할 일 등록 · 원본 `archive/20260702_002952_shortcut-customization-todo.md`
- 요구: 하드코딩 단축키를 나중에 사용자 변경 가능하게 → 할 일 등록(코드 변경 없음, 설계/백로그만).
- docs/26 §5-4 "현재 하드코딩 단축키→재정의 대상" 표 신설, §7-β·§8에 명령 레지스트리 이관 명시. docs/04에 "단축키 커스터마이즈"·"명령 팔레트" roadmap 승격.
- 계획: 동작 코드→명령 `Execute` 이동, `OnGridKeyDown`=Keymap 조회, 기본 키맵+`keybindings.json` 덮어쓰기, net8.0 분리(맥 테스트). 시점: P1 설정 시스템(β).

## 00:24:27 · Alt+↓ 항목 활성화 (F19) · 원본 `archive/20260702_002427_alt-down-activate.md`
- 요구: Alt+아래 — 디렉터리 진입/파일 실행.
- `OnGridKeyDown`: `Down && IsAltDown() && cur>=0`→`ActivateItem(활성패널, 캐럿)`. `ActivateItem`: 폴더/심볼릭→Navigate, 파일→Launcher(실패는 상태바 격리). 활성 패널 기준.
- docs/19 F19. 후속: Enter 활성화(옵션).

## 00:15:00 · 펼침 상태 "유지" 용어 정정 · 원본 `archive/20260702_001500_expansion-state-terminology.md`
- 피드백: F18은 "복원(restore)"이 아니라 **상태 유지(maintain)** — 구현 일치 확인.
- 확인: 펼친 경로를 패널별 집합(`_leftExpanded/_rightExpanded`)에 계속 보유→어느 경로로 이동해도 유지(내부는 재적용, 사용자 관점=유지). 접힘은 그 폴더만 제거(자손 보존). 요구 일치.
- 정정: docs/19 F18 "복원"→**"유지"**(제목·설명·주석). 동작 변경 없음(용어/문서만).

## 00:06:47 · 펼침 상태 기억 & 진입 복원 (F18) · 원본 `archive/20260702_000647_expansion-state-memory.md`
- 요구: 하위에 펼친 폴더 상태를 저장했다 그 폴더 진입 시 동일 표시(경로 기준·패널별). F18, FR-X4 일부.
- 구현: `HashSet<string> _leftExpanded/_rightExpanded`(OrdinalIgnoreCase). `SetExpanded` 리팩터(+`ExpandInPlace`/`CollapseInPlace`+`ApplySavedExpansion` 재귀). `LoadDirectory`가 자식 로드 후 재귀 복원. 원리: 펼침이 자식을 아래 삽입→인덱스 루프가 방문→임의 깊이 복원.
- 검증: CI(windows) app job green. 한계: 세션 영속화(JSON) 미포함(인메모리)·코어 `VisibleRow`(C1) 이관 시 통합.
