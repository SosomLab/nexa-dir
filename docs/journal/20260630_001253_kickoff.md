# 작업 기록 — 2026-06-30 00:12:53 (KST)

> 기록 ID: `20260630_001253_kickoff`
> 단계: 설계/계획 (코드 미구현) · 다음: 설계 충분히 진행 후 개발 착수

---

## 1. 목표 (Goal)

- **차세대 Windows 탐색기 프로그램 개발** (`nexa-dir`, 원격 `SosomLab/nexa-dir`).
- 이번 방향: **설계부터 충분히 진행한 뒤 개발로 이어간다.**

## 2. 질문 & 답변 (Q&A)

### Q1. 기술 스택 (개발 환경 macOS / 타깃 Windows 탐색기)
- **답변:** "성능을 최대한 올릴 수 있는 네이티브로"
- **반영:** 네이티브 + 최대 성능. 권장안 = **Rust 코어(cdylib) + WinUI 3(C#/.NET) UI** 하이브리드
  (핫패스는 Rust, UI는 모던 네이티브). → ADR-0001, `docs/01-architecture.md`
  - 미확정 분기: 순수 C++/WinRT(최고 성능·난이도) vs 순수 C#/.NET WinUI(최고 생산성) — 사용자 선택 시 ADR 수정.

### Q2. '차세대' 핵심 차별화 기능 (복수 선택)
- **답변:** 탭+듀얼 패널 / 빠른 미리보기 / 클라우드·원격 통합 / AI 검색·정리 **모두 필요**,
  최신 트렌드는 TODO에 추가, **우선은 탭/듀얼부터 구현**.
- **반영:** 전체를 로드맵에 배치, **Milestone 1 = 탭 + 듀얼 패널**.
  트렌드 백로그 문서(`docs/04-trends-todo.md`) 신설.

### Q3. 이번 단계 진행 범위
- **답변:** "설계/계획만 먼저" → (후속) "설계부터 충분히 진행하고 개발을 진행할꺼야"
- **반영:** 설계 문서 세트 작성 완료, 이후 설계 보강 → 개발 착수.

## 3. 진행 사항 (Done)

- [x] 저장소 상태 확인 — 빈 리포(커밋 없음), 원격 `SosomLab/nexa-dir`.
- [x] 요구사항 확정 (Q&A 3건).
- [x] 설계 문서 세트 작성:
  - [x] `README.md` — 개요·원칙·문서 인덱스
  - [x] `docs/00-vision.md` — 비전·목표·경쟁 분석·차별화
  - [x] `docs/01-architecture.md` — 스택 결정(ADR-0001)·시스템 아키텍처·모듈
  - [x] `docs/02-roadmap.md` — M0~M6 로드맵 (M1=탭/듀얼 1순위)
  - [x] `docs/03-features.md` — 기능 상세 명세
  - [x] `docs/04-trends-todo.md` — 최신 트렌드 백로그
- [x] 프로젝트 메모리 기록.

## 4. 핵심 결정 요약 (Decisions)

| # | 결정 | 근거 |
| --- | --- | --- |
| D1 | 하이브리드 네이티브: Rust 코어 + WinUI 3 | 핫패스(I/O·인덱싱·디코딩) 네이티브로, UI는 생산적 네이티브 |
| D2 | 구현 1순위 = 탭 + 듀얼 패널 (M1) | 사용자 지정 핵심 워크플로우 |
| D3 | VFS provider 모델로 로컬/클라우드/원격 통일 | 동일 UI에서 어디서나 탐색 |
| D4 | 가상화·스트리밍 열거로 대용량 폴더 대응 | 첫 렌더 <150ms 목표 |

## 5. 미해결 / 결정 대기 (Open)

- [ ] **O1.** 네이티브 언어 최종 확정(하이브리드 vs 순수 C++ vs 순수 C#) — 권장: 하이브리드.
- [ ] **O2.** AI 제공자(로컬 모델 vs 외부 API) 및 프라이버시 정책 — M5 착수 전 ADR.
- [ ] **O3.** 라이선스 결정 (README TBD).
- [ ] **O4.** Windows 빌드 환경 확보 방안(물리 PC / VM / CI 전용).

## 6. 다음 단계 (Next)

1. 설계 보강: O1 확정 → 필요 시 ADR 수정.
2. M0 상세 설계(인터롭 API 표면, VFS 인터페이스, 가상화 렌더 데이터 흐름).
3. 리포 스캐폴딩(`core/` Rust 워크스페이스, `app/` WinUI 솔루션, CI).
4. 개발 착수: M0 → M1(탭/듀얼 패널).

---

*이 기록은 작업 진행에 따라 새 타임스탬프 파일로 누적한다. (`docs/journal/YYYYMMDD_HHMMSS_*.md`)*
