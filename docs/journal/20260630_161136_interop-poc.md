# 작업 기록 — 2026-06-30 16:11:36 (KST)

> 기록 ID: `20260630_161136_interop-poc`
> 이전 기록: `20260630_114743_git-identity-and-visibility`

## 1. 요구
- M0 첫 기능 단위: **인터롭 Rust↔C# 왕복 PoC** 구현.
- 빌드 통합(cdylib→앱) 내용을 docs/18에 통합.

## 2. 구현 (수직 슬라이스, 초안→확장)

### 단위 1 — Rust 측 (커밋 c41dc41)
- `nexa-interop`에 C ABI 함수 `nexa_poc_add(a, b) -> a.wrapping_add(b)` + 테스트(3 assert).
- 게이트: fmt/clippy(-D warnings)/test green.

### 단위 2 — C# 측 + 빌드 통합
- **P/Invoke**: `app/Nexa.App/NativeInterop.cs` — `nexa_abi_version`, `nexa_poc_add` (Cdecl).
- **왕복 호출**: `MainWindow`에서 호출 → `StatusText`에 결과 표시, 실패는 try/catch로 격리(앱 비중단).
- **빌드 통합(csproj)**: `BuildNexaInterop`(cargo build -p nexa-interop) → `CopyNexaInterop`(nexa_interop.dll → 앱 출력).
  Debug↔debug / Release↔release 프로필 매핑. 전제: cargo가 PATH.
- **CI**: app job에 Rust 툴체인 + rust-cache 추가(앱 빌드가 코어 빌드에 의존).
- **문서**: docs/18 §2-1(인터롭 통합) 신설 + 요약표·CI표 갱신.

## 3. 검증
- `dotnet build app/Nexa.App -c Debug` → cargo 실행 + dll 복사 + 앱 빌드 0 error.
- dll 복사 확인: `bin/Debug/.../nexa_interop.dll`(109KB).
- **실제 P/Invoke 왕복**(PowerShell Add-Type): `abi=1`, `poc_add(2,3)=5`, `(-1,1)=0`, `(40,2)=42`.
  - 주의: PowerShell native 로드는 PWD가 아닌 `Environment.CurrentDirectory`/PATH 기준 → 절대경로 지정 필요(검증 하니스 한정 이슈, 앱은 실행파일 옆 dll 자동 로드).

## 4. 다음
- 인터롭 표면 확장: 문자열/버퍼 반환(소유권·해제 규약), 핸들 기반 API, 이벤트 스트림.
- 또는 다음 단위: `feat(core/vfs)` 로컬 디렉터리 스트리밍 열거(맥 가능).
