# 2026-07-05 · 작업 기록 (시간 역순)

> 일자 단위 기록(신규 규약). **최신이 위**. 요약은 [DEVLOG.md](../DEVLOG.md), 기능/마일스톤은 [MILESTONES.md](../MILESTONES.md).

## feat/bottom-panel-info — 하단 정보/미리보기 뷰 (BP-2) 착수
- 브랜치 `feat/bottom-panel-info` 생성(분기: main). BP-1(컨테이너) 위에 콘텐츠 실체화.
- **BP-2 슬라이스 1**: 하단 도킹 "정보" 탭을 현재 폴더 → **선택/캐럿 항목 속성**(이름·종류·크기·수정 날짜시간·경로)으로 확장. 다중 선택=개수, 선택 없음=현재 폴더. 선택 변경(`UpdateSelectionCount`)·네비 시 `RefreshBottomDocks` 갱신.
- 다음: 총 크기(다중 선택)·간단 미리보기(텍스트/이미지)·Hex. 터미널(BP-T)은 에뮬레이터 방식 결정 후.

## 아카이브 → 일자 파일 재구성
- 과거 세션별/시각별 저널(`YYYYMMDD_HHMMSS_*`)·라운드 워크로그를 `journal/archive/`로 이동(67개, git rename).
- 아카이브 내용을 **일자 단위 파일로 요약·재구성**: `2026-06-30`~`2026-07-04.md`(시간 역순 상세) 생성. 06-30/07-01/07-02는 병렬 에이전트로 아카이브를 읽어 요약, 07-03/07-04는 커밋 이력(시각) 기준.
- 활성 `journal/`에는 일자 파일만, 상세 근거는 `archive/`에 보존.

## 문서 통폐합 · 기록 규약 정립
- 시간순 기록 → [DEVLOG.md](../DEVLOG.md), 목적/기능·마일스톤 → [MILESTONES.md](../MILESTONES.md)로 통폐합.
- 앞으로 기록은 **일자 단위 파일**(`journal/YYYY-MM-DD.md`, 파일 내 시간 역순)로만. CLAUDE.md §6/§7 규약 갱신.
- 과거 세션별/시각별 저널·라운드 워크로그는 아카이브로 보존.

## 브랜치 정리
- 병합 완료 브랜치 삭제(로컬+원격): `refactor/001~003-audit` · `feat/bottom-panel` · 스트레이 `a`. 이력은 [BRANCHES.md](../BRANCHES.md)에 보존.

## feat/bottom-panel — 하단 패널 BP-1 → main 병합(`01c85a7`)
- BP-1a 콘텐츠 호스트 `BottomDockView`(정보/미리보기/Hex/터미널 종류 선택·스왑, 정보=현재 폴더).
- BP-1b **Ctrl+`** 표시/숨김 토글. BP-1c 하단 패널 상태(표시/높이/분리/종류) `session.json` 저장·복원.
- 미리보기/Hex/터미널(ConPTY)은 후속(BP-2/BP-T). 브랜치 생성 17:47 → 병합 18:02.

## refactor/003-audit → main 병합(`bd45f86`)
- 3차 감사 라운드 전체(77커밋). 상세 [refactor-003-worklog](archive/refactor-003-worklog.md).

## 파일 전송 단일 엔진 + 부가(003 브랜치 세션)
- 전송 단일 엔진 통일(DnD+붙여넣기=`TransferPathsInto`): 덮어쓰기 확인(예/모두 예/건너뛰기/취소)·바이트 진행률·진행 창(맨앞·취소·자동닫기 off)·확인 프롬프트 창 안 embed(XamlRoot 오류 해결).
- OS 클립보드 붙여넣기(탐색기 복사 인식) · 외부 DnD StorageItems(파일 열기)/Alt=경로.
- 탭 세션 저장/복원(요청/수행 분리·Tick 코얼레싱) · 새로 만들기(폴더/파일/바로가기) · 수정 날짜/시간 컬럼(DateTime) · 경로 바 환경변수 해석 · cargo fmt · 이 PC 식별 문서.
