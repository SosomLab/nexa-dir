# 작업 기록 — 2026-07-01 12:03:01 (KST)

> 기록 ID: `20260701_120301_a2-column-header`
> 이전 기록: `20260701_115059_nexa-controls-product`

## 1. 요청
- 구현 순서대로 진행 — **A2**: `NexaFileGrid` 컬럼 헤더 + 컬럼 모델([23](23-column-system.md)).

## 2. 구현 (A2 초안)
- **`NexaGridColumn`**(Nexa.Controls): 컬럼 정의(도메인 비종속) — `Header`/`Width`/`Key`. 셀 값은 행 템플릿(도메인 주입)이 렌더.
- **`NexaFileGrid`**: 2행 Grid(헤더 Auto + 본문 *). 헤더 = `Columns`를 가로 `ItemsRepeater`로 렌더(Header + Width + 구분선). 본문 = 기존 가상화 `ItemsRepeater`(`ItemTemplate`). `Columns`(get-only `IList`) — XAML에서 채움.
- **앱**: 좌/우 `NexaFileGrid`에 컬럼(종류64/이름240/크기100) 정의. 본문 행 템플릿을 `StackPanel`+고정 너비(64/240/100)로 바꿔 **헤더와 정렬**(이름은 `TextTrimming`).

## 3. 검증
- `dotnet build app/Nexa.App -c Debug` → **0 Warning / 0 Error**.
- Nexa.App.exe 기동 → 좌/우 패널에 **컬럼 헤더(종류/이름/크기)** + 본문 정렬 표시, 예외 없음.

## 4. 한계 / 다음
- 현재 헤더-본문 너비는 **고정값으로 수동 정렬**. **A3(리사이즈)** 에서 헤더 드래그 + 본문 연동(공유 너비)으로 대체.
- A4(좌/우 동기화 — 요구4), A5(정렬) 이어서.
- 셀 값 완전 추상화(`ICellValueProvider`)는 컬럼-본문 통합 시(A3~) 도입.
