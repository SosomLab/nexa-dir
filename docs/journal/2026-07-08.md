# 2026-07-08 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).
> **⏱ 표기 = 해당 단위의 git 커밋 시각 범위(KST, 초 단위 원천=git log)** — 실제 착수는 첫 커밋 이전(직전 단위 종료 직후).

## 터미널 위치 정확도 라운드 + 선택·복사·붙여넣기 (실기 QA 통과)

> ⏱ 2026-07-08 09:55:18 ~ 10:33:26 (`8bfc1c0`→`fb8f33f`, 5커밋)

첫 로딩 상단 잘림·캐럿/입력 위치 불일치를 실기 재현과 함께 5단계로 추적·해결(전 항목 사용자 QA 통과):

1. **초기 크기 어긋남**(`8bfc1c0`, 09:55): 탭 첫 활성 직후 레이아웃 전(크기 0)에 세션 시작 → 최소 격자 20×5로 ConPTY 오픈 후 리사이즈되며 초기 출력 어긋남 + 상단 패딩 4px이 최하단 스크롤에 잘림. → **Start()를 첫 실측 이후로 지연**, 세로 패딩 0, 열 계산에 좌우 패딩 제외.
2. **스크롤 시퀀스 미구현**(`d8903f7`, 10:08): 프롬프트 중복 잔존·캐럿 행 어긋남(reverse-i-search에서 두드러짐) — ConPTY의 SU/SD(`CSI S/T`)·IND/NEL(`ESC D/E`) 무시. → ScrollUp/Down(스크롤백 보존)+IND/NEL/CNL/CPL 구현.
3. **DECSTBM(스크롤 마진)**(`3ec1080`, 10:19): "clear 후 정확" 관찰 = 스크롤 누적 어긋남 → `CSI r` 마진 추적 + LF/RI/SU/SD를 영역 스크롤로(부분 마진=영역만·스크롤백 미보존).
4. **레이아웃 반올림 누적**(`f45ed5d`, 10:29) — 최종 근본 원인: LineH=17.55(비정수)를 StackPanel 누적 배치 + UseLayoutRounding이 줄마다 18px로 반올림 → **행당 ~0.45px 드리프트 누적**(40행≈"1.8칸" ✓, 위 정상·아래 어긋남 ✓, 출력량 비례 ✓, clear 리셋 ✓). → 줄을 **Canvas 절대 배치(index×LineH)** 로 — 텍스트·캐럿·선택·HitCell 동일 수식(비누적).
5. **정렬 함정 재발**(`fb8f33f`, 10:33): Canvas 명시 크기+Stretch=가운데 배치 → 초기/clear 직후 짧은 줄 폭 때문에 우측 치우침. → Left/Top 고정.

**신규 — 터미널 선택·복사·붙여넣기**(`3ec1080`): 마우스 드래그 선택(절대 라인 기준 하이라이트, 새 출력에도 유지) · **Ctrl+C=선택 시 복사/없으면 SIGINT**(WT 관례)·Ctrl+Shift+C=항상 복사 · **Ctrl+V=붙여넣기**(개행→CR) · `VtScreen.GetText`(전각 연속 셀 제외·우측 트림·CRLF).

## 테마 시스템 S1 + 상태바 정리 + 가로 스크롤 (`feat/theme-system` → main 병합)

> ⏱ 2026-07-08 08:56:51 ~ 09:10:11 (`08095a3`→`a099352`, 3커밋)

**추가 단위(같은 브랜치):**
- **패널 보기 토글 → 표시(S) 메뉴 이관**(`c5df6b9`, 09:05:58): 상태바의 "표시: 런처/우 패널/하단/분리" ToggleButton 4종 제거 → 표시 메뉴 체크 항목. **상태바 = 상태 텍스트 전용**. 상태 원천을 메뉴 IsChecked로 교체(세션 캡처/복원·Ctrl+`·스냅 가드·UpdateBottomDock — 분리의 IsEnabled 연동은 정책 유지·선호 항상 저장으로 대체).
- **파일 목록 가로 스크롤**(`a099352`, 09:10:11): 본문 가로 `Auto`(컬럼 총폭이 뷰포트를 넘을 때만 표시 — 세로와 동일 규약) + **헤더 숨김 ScrollViewer 양방향 동기**(리사이즈/정렬 유지) + 러버밴드 가로 오프셋 보정. 이름 컬럼을 넓혀도 다른 컬럼을 스크롤로 확인 가능.
- 사용자 QA: 라이트 배치 "전반적으로 깔끔" 확인.

- **[docs/39-theme-system.md](../39-theme-system.md) 신설**: 문제(영역 구분용 임시 틴트)·아키텍처(WinUI ThemeDictionaries + 시맨틱 토큰 + RequestedTheme)·토큰 표(Light/Dark 10종)·**후속 세부 설정 UI 설계**(모양 페이지 — 테마팩[토큰 색 오버라이드]·폰트·크기/밀도, settings.json 합류).
- **App.xaml `ThemeDictionaries`**: `Nexa*Brush` 토큰 10종(Window/Chrome/Panel/TabBar/Header/Field/BottomDock/StatusBar/Border/Accent) — Light/Dark 팔레트.
- **틴트 전면 제거 → 토큰 치환**(MainWindow.xaml): 도구모음(파랑)·런처(초록)·좌 패널(파랑)·우 패널(빨강)·탭 바(파랑/빨강)·하단 도킹(청록/자홍)·상태바(회색)·경로바 필드·스플리터 → **중립 배경 + 경계선**으로 영역 구분. 그리드 컬럼 헤더 `HeaderBackground` 토큰 바인딩, `NexaFileGrid` 헤더 구분선 중립화, `PanelTab` 탭 배경 중립 알파(활성=accent 30%/비활성=회색 8% — 양 모드 공용).
- **모드 선택**: `ThemeOptions.Mode`(System/Light/Dark, 기본 **Light** — 라이트 팔레트 검증용, DR-2 다크 기본은 다크 정비 후 재결정) + **구성(O) 메뉴 "테마: 시스템/라이트/다크"**(라디오식) + `ApplyTheme()`(RequestedTheme).
- 테마 비대상: 임베디드 터미널(다크 고정 — 터미널 정체성)·행 선택/호버(알파 공용).

## 앱 아이콘 제작·적용 — 트리 다중선택 탐색기 + `</>` 개발자 컨셉

> ⏱ 2026-07-08 08:14:05 ~ 08:16:33 (`de1389f`·`4f81f82`)

- **`scripts/make-app-icon.ps1`** 신규 — GDI+ 생성기(외부 도구 불요·재현 가능): 다크 라운드 배경(DR-2) + **폴더 루트에서 뻗는 트리** + **다중 선택(accent) 행 2개** + 우상단 **`</>` 글리프**(개발자 지향). 1024px 마스터 → 고품질 다운스케일 **PNG 9종(16~1024) + 멀티사이즈 ICO(16~256, PNG 엔트리)**.
- 적용: csproj `ApplicationIcon`(exe 임베드 — 탐색기/작업표시줄) + ico Content 배포 + `MainWindow` `AppWindow.SetIcon`(창 타이틀바).
- 파일명 `nexa-*` → **`nexa-dir-*`** 전체 변경(`4f81f82`) — "nexa"는 여러 프로그램 공용 브랜드라 제품명으로 구분.
- 트리비아: PowerShell 배열 언롤링으로 ICO 블롭 유실(125바이트) → `return , $bytes`(NoEnumerate)로 수정.

## breadcrumb 긴 경로 — 최근 세그먼트 표시 (`57b489d`)

> ⏱ 2026-07-08 02:18:10 (`57b489d`)

- 경로가 바 폭을 넘으면 **끝(가장 깊은 폴더)으로 스크롤**(오른쪽 정렬 효과) — 세그먼트 재구성 시 + 창 리사이즈 시 유지. Low 우선순위 enqueue로 레이아웃 확정 후 오프셋 적용.

## 탭 컨텍스트 메뉴·잠금·고정 (TAB-MENU, `288d36e`→`32e8935` 5커밋)

> ⏱ 2026-07-08 02:02:28 ~ 02:13:32

- **탭 우클릭 메뉴**: 새 탭 / 탭 닫기(마지막·잠금 비활성) / 모든 탭 닫기(현재 제외 — **잠긴 탭 보존**, 활성 탭 닫히면 현재 탭 전환) / 탭 옵션(잠금·고정 체크 토글). **우클릭 시 해당 탭 먼저 활성화**(`ca07d04`).
- **탭 잠금(IsLocked)**: 닫기 동작 전체(우클릭·더블클릭·Ctrl+W) 제외 — `CloseTab` 가드. **열쇠 아이콘 = 탭 오른쪽 끝**.
- **탭 고정(IsPinned)**: **핀 아이콘 = 폴더명 앞(왼쪽 끝)** + 핀 그룹(맨 앞) 이동 — 핀 여러 개면 마지막 핀 뒤 삽입, 해제 시 그룹 바로 뒤(동일 삽입식). 드래그 재정렬에도 핀 그룹 경계 클램프. (동시 설정 = 양 끝 각각 표시 — 나란히→합성 배지→**양 끝** 3회 반복 후 사용자 확정.)
- **긴 탭 제목 말줄임(…)**: 배지 추가로 넣었던 가로 StackPanel이 자식에 무한 폭을 줘 `TextTrimming` 죽음 → **Auto(핀)+*(제목)+Auto(잠금) Grid**로 교체·복원(`32e8935`).
- 세션 영속: `TabSession.Locked/Pinned`(기본 false — 과거 파일 호환) 캡처·복원.

## 탭 드래그 재정렬·패널 간 이동 (TAB-DND, `0e93f06`·`f31a433`)

> ⏱ 2026-07-08 01:46:55 ~ 01:56:38

- 탭 드래그로 위치 변경: 같은 스트립 재정렬 + **좌우 패널 간 이동**. 수정키 = 파일과 동일(기본 이동·**Ctrl=복제**[같은 경로 새 탭]·Shift=이동). **탭 영역에서만 유효**(그 외 🚫), 마지막 탭의 패널 밖 이동 금지. 패널 간 이동 시 원본=이웃 활성·이동 탭=대상 활성.
- **삽입 위치 하이라이트 + 맨 끝 이동**(`f31a433`): 탭 좌/우 절반 기준 앞/뒤 삽입 + **바 빈 영역=맨 끝**, 삽입 지점에 accent 세로 바 실시간 표시. XAML(비상승)·OLE 폴백(상승) 두 경로 공용 계획(`TabDropPlanAt`).
- 부수: **경로·항목 수 헤더 토글**(`0e93f06`) — 경로 바 아래 "현재 경로 — N개 항목" 줄 기본 감춤, 표시(S) 메뉴 토글(`ShowPathHeader`, 설정 UI 후속).

## 러버밴드(마퀴) 다중 선택 + 행 히트영역 (B-4 부분, `6bb779f`)

> ⏱ 2026-07-08 01:20:44 (`6bb779f`)

- **시작 규칙**(사용자 확정): 선택된 행 드래그=파일 DnD / **미선택 행 드래그=러버밴드** / **빈 공간 드래그=선택 있어도 러버밴드**. press 시점 선택 상태로 행 `CanDrag` 토글.
- **행 히트영역 = 컬럼 총폭**(행 루트 `HorizontalAlignment=Left`) — 크기 컬럼 뒤 공간이 배경(빈 영역)으로 동작(탐색기 동일). `BodyScroll Background=Transparent`로 배경 히트 보장.
- 그리드 마퀴: 4px 임계(클릭/더블클릭 보존, 임계 전 미캡처) → 캡처+밴드 오버레이+**연속 인덱스 범위** 통지(행 높이 균일 → 가상화 무관 정확) → 호스트가 anchor+SelectRange 반영. 밴드 x가 행 폭과 겹칠 때만 선택. 가장자리 자동 스크롤(DnD 타이머 공용).

## 통합 실기 QA 라운드 + 금지 글리프 수정 · 전송 창 카운트다운

> ⏱ 2026-07-08 00:40:16 ~ 01:05:38 (`ea27487`→`8494c70`, 4커밋)

- **실기 QA(사용자) 추가 확인**: 전송 창 **2초 카운트다운 자동 닫기** 정상 · **"Send to" 서브메뉴 채움 정상**(Bluetooth/압축/바탕화면/문서/메일 = IContextMenu2/3 포워딩 검증 통과) · 이름변경 중 빈영역 클릭 커밋 정상 · 빈영역 우클릭 선택해제 정상 · **이름변경 undo 정상**(이로써 B-13u 5종 전부 확인) · 외부→빈영역/폴더 행 드롭 정상 · 자기 하위 폴더 이동 차단 동작 확인.
- 🐞 **금지 글리프 미표시 수정**: 자기/하위 폴더 드래그 시 차단은 되나 시각 피드백 없음 — `ApplyDragCaption`/그리드 본문 핸들러가 **모든 경우 `IsGlyphVisible=false`를 선설정**해 금지(None) 시 OS 금지 글리프(🚫)까지 숨겼던 것. **None일 때만 글리프 켜고** 수락 시엔 기존대로 캡션 대체.
- **전송 진행 창 자동 닫기**(`ea27487`→`35bf10c`): 성공 완료 시 닫기 버튼 "닫기 (2)→(1)" **2초 카운트다운 후 자동 닫힘**(클릭=즉시). 실패/취소는 유지. `AutoCloseTransferWindow` 기본 true.

## B-13u Undo/Redo — 실기 QA 통과·마감 (셸 삭제 가로채기 + 배경 메뉴 항목)

> ⏱ 2026-07-08 00:25:35 ~ 00:35:15 (`4cdb708`→`0f55ca0`, 3커밋)

- **실기 QA(사용자) 전부 확인**: 삭제(휴지통→복원) · 복사 · 이동 · 새 폴더 생성 각각 Ctrl+Z/Y 정상. **B-13u S1+S2 검증 완료.**
- **셸 메뉴 "삭제" 가로채기**(`8c6816d`): 셸 메뉴 삭제가 `InvokeCommand`로 셸이 직접 수행 → `DeleteBatchOp` 미기록 → Ctrl+Z가 이전 작업을 되돌리던 버그. `ShellContextMenu`에 **`verbInterceptor` 훅**(선택 후 `GetCommandString(GCS_VERBW)`로 canonical verb 조회) 추가, 호스트가 **"delete" 동사를 `DeletePaths`(휴지통)로 라우팅** — undo 기록·상태바·재로드 통합. 미구현 확장(null verb)은 기존대로 셸 실행. (훅은 후속 cut/copy/paste 앱 클립보드 통합에도 재사용 기반.)
- **빈 영역 메뉴에 실행 취소/다시 실행**(`4cdb708`): "실행 취소: {마지막 작업 설명}"·"다시 실행: …" + Ctrl+Z/Y 표기, 히스토리 없으면 비활성 — 탐색기 배경 메뉴 동일.
