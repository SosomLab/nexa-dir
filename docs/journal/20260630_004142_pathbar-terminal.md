# 작업 기록 — 2026-06-30 00:41:42 (KST)

> 기록 ID: `20260630_004142_pathbar-terminal`
> 이전 기록: `20260630_003833_plugin-architecture`

## 1. 신규 요구 (Path Finder 스크린샷 참조)

### R9. 상단 계층 경로 바 — 우선 구현
- **요구:** 상단에 폴더 위치를 계층적으로 분리해 보여주는 뷰(브레드크럼)를 **우선 구현 대상**으로.
- **반영:** FR-A2를 ★우선 구현으로 승격(세그먼트 클릭 이동·형제 드롭다운·원시 경로 편집·드롭 타겟·동기·축약),
  `03 §2-A` 상세, 로드맵 M1 "계층 경로 바" 우선 항목 추가.

### R10. 하단 임베디드 터미널 패널 — 설계 우선, 구현 후속
- **요구:** 하단 터미널(cmd/pwsh) 통합은 **설계 우선 반영, 향후 구현**.
- **반영:** FR-K2b 신설(설계 동결/구현 P2), `03 §3-K2b` 상세 설계
  (하단 도킹·다중 탭·경로 동기·**ConPTY** 호스팅·크래시 격리·PTY 이식 추상화), 로드맵 M1에 "설계 동결, 구현 후속".

## 2. 결정 (Decisions)
| # | 내용 |
| --- | --- |
| D14 | 계층 경로 바(브레드크럼)는 M1 P0 우선 구현 — 세그먼트 이동/형제 전환/경로 편집/드롭 |
| D15 | 임베디드 터미널 패널은 M1에 **설계만 동결**, 구현은 후속. ConPTY 호스팅, 코어 `pty` 모듈로 이식 격리 |
| D16 | M1 외부 터미널 실행(런처)과 임베디드 패널(후속)을 분리 |

## 3. 진행 (Done)
- [x] FR-A2 승격 + FR-K2b 신설, `03 §2-A`/§3-K2b 설계, 로드맵 M1 반영.

## 4. 다음 단계
- OD1 언어 확정 → M0 스파이크 + CI 스켈레톤. (경로 바·플래그십·런처는 M1 착수 1순위 묶음)
