# 작업 기록 — 2026-07-01 15:55 (KST)

> 기록 ID: `20260701_155500_inline-tree-finder-columns`
> 이전 기록: `20260701_151200_f9-splitter-snap-menu-backlog`

## 1. 요청 (연속)
- 스플리터 2px→비교→4px, 헤더 밝은 회색, snap 20px, Alt로 스냅 임시 해제, 스플리터 그립 숨김, 최소 크기 지정.
- 컬럼 설정 모달 + 헤더 드래그 재정렬을 **설계에 반영**.
- 헤더 색을 설정에서 바꿀 수 있게(HeaderBackground).
- **디렉토리 인라인 펼침** + 목표 이미지(Finder 스타일) 수준으로 구성.

## 2. 설계 (docs/23 §6 신설)
- **컬럼 설정 모달**: 전체 컬럼 나열 + 표시여부/너비/순서(드래그) + 적용범위(이 패널/양쪽/폴더 프로파일) + 미리보기. 와이어프레임 포함.
- **헤더 직접 드래그 재정렬**: 리사이즈 핸들(우측 8px)과 영역 분리, `Columns` 순서=표시 순서, `ColumnReordered` 이벤트.
- **모델·경계**: `NexaGridColumn`에 `Visible` 추가 예정, 표시/순서 패널별 독립화는 후속 리팩터.

## 3. 구현
### 3-1. 헤더 색상 설정 속성 (Nexa.Controls)
- `NexaFileGrid.HeaderBackground` **의존성 속성**(기본=밝은 회색 `#808488`) + 전폭 헤더 바. 설정에서 바인딩/오버라이드 가능.

### 3-2. 스플리터 개선 (app)
- 4px 고정(Min/Max) + `Foreground=Transparent`(그립 숨김). 스냅 임계 20px. **Alt 드래그=스냅 임시 해제**(`InputKeyboardSource`). 좌/우·하단 최소 크기(160/80), 숨김 시 MinWidth 0.

### 3-3. 인라인 폴더 펼침 + Finder 컬럼 (F10, 플래그십 초안)
- `DirItem` 트리 모델화(Depth/FullPath/IsExpanded + 표시용 파생속성). `ReadDir(path, depth)` 폴더 우선 정렬.
- 컬럼 = 이름·수정한 날짜·종류·크기. 이름 셀 = 들여쓰기 + 디스클로저(▶/▼) + 파란 폴더 아이콘 + 굵은 폴더명.
- 패널별 `ObservableCollection<DirItem>` + `OnToggleExpand`(자식 삽입/자손 제거).

## 4. 검증
- `dotnet build app/Nexa.App -c Debug` → **0/0**. 실행: 폴더 우선·아이콘·펼침 동작, 헤더 밝은 회색, 그립 없음.

## 5. 다음 (백로그)
- 헤더 **드래그 재정렬**(본문 컬럼-구동化) → **컬럼 설정 모달** → 코어 **가시행 스트림(C1)** → 교차 다중 선택(C3).
- **설정 시스템(JSON)**: 스플리터 위치·컬럼(너비/표시/순서, 폴더별)·헤더색 저장/복원.
- 헤더 크기 변경(컬럼 폭)은 이미 구현됨(핸들 드래그).
