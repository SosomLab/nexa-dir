<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Nexa.Controls.NexaPathBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nexa.Controls"
    HorizontalContentAlignment="Stretch">

    <UserControl.Resources>
        <!-- 경로 편집기: 기본 TextBox(MinHeight 32·헤더 chrome)가 경로바 높이를 키워 화면을 늘리므로
             브레드크럼 높이를 유지하는 최소 템플릿(테두리 + ContentElement만). -->
        <Style x:Key="PathEditor" TargetType="TextBox">
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Padding" Value="4,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="{ThemeResource ControlFillColorDefaultBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ScrollViewer x:Name="ContentElement"
                                          Padding="{TemplateBinding Padding}"
                                          VerticalContentAlignment="Center"
                                          HorizontalScrollMode="Auto" VerticalScrollMode="Disabled"
                                          HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"
                                          IsTabStop="False" ZoomMode="Disabled"
                                          AutomationProperties.AccessibilityView="Raw" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- 브레드크럼(기본) ↔ 편집(TextBox) 2모드. 우클릭 시 편집 진입. -->
    <Grid>
        <!-- 브레드크럼: 세그먼트 가로 배치 + 넘치면 수평 스크롤 -->
        <ScrollViewer x:Name="PART_Scroll"
                      HorizontalScrollMode="Auto" HorizontalScrollBarVisibility="Hidden"
                      VerticalScrollMode="Disabled" VerticalScrollBarVisibility="Disabled"
                      VerticalAlignment="Center"
                      RightTapped="OnBreadcrumbRightTapped">
            <ItemsRepeater x:Name="PART_Breadcrumb">
                <ItemsRepeater.Layout>
                    <StackLayout Orientation="Horizontal" Spacing="0" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local:PathSegment">
                        <!-- 구분자 \ 와 폴더명을 바짝 붙여 fullpath처럼 보이게(세그먼트 라벨 좌우 패딩 최소) -->
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{x:Bind Prefix}" Opacity="0.5" VerticalAlignment="Center" />
                            <Border Tag="{x:Bind}" Background="Transparent" CornerRadius="3" Padding="1,1"
                                    PointerEntered="OnSegPointerEntered" PointerExited="OnSegPointerExited"
                                    Tapped="OnSegTapped">
                                <TextBlock Text="{x:Bind Label}" VerticalAlignment="Center" />
                            </Border>
                        </StackPanel>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>

        <!-- 편집 모드: 전체 경로 텍스트(전체 선택·복사/붙여넣기). Enter=이동, Esc/포커스아웃=취소 -->
        <TextBox x:Name="PART_Editor" Visibility="Collapsed" VerticalAlignment="Center"
                 Style="{StaticResource PathEditor}"
                 KeyDown="OnEditorKeyDown" LostFocus="OnEditorLostFocus"
                 TextChanged="OnEditorTextChanged" />

        <!-- 폴더 제안 드롭다운(탐색기식 주소 자동완성) — 편집 중 구분자/접두사 입력 시 하단 표시.
             AllowFocusOnInteraction=False: 클릭해도 편집기 포커스 유지(LostFocus로 편집이 닫히지 않게 — AutoSuggestBox와 동일 기법) -->
        <Popup x:Name="PART_SuggestPopup" IsLightDismissEnabled="False" ShouldConstrainToRootBounds="False">
            <Border Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="4" Padding="2"
                    MaxHeight="280">
                <ListView x:Name="PART_SuggestList" IsItemClickEnabled="True" SelectionMode="Single"
                          AllowFocusOnInteraction="False"
                          ItemClick="OnSuggestItemClick">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="MinHeight" Value="0" />
                            <Setter Property="Padding" Value="8,3" />
                            <Setter Property="AllowFocusOnInteraction" Value="False" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Border>
        </Popup>
    </Grid>
</UserControl>
