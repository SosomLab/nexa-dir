<Window
    x:Class="Nexa.App.TransferProgressWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="파일 전송">

    <Grid Padding="20" RowSpacing="10" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Grid.Row="0" FontSize="16" FontWeight="SemiBold" Text="전송 중…" />
        <TextBlock x:Name="DetailText" Grid.Row="1" Opacity="0.7" TextTrimming="CharacterEllipsis" Text="" />
        <TextBlock x:Name="PercentText" Grid.Row="2" FontSize="26" Text="0%" />
        <ProgressBar x:Name="Bar" Grid.Row="3" Minimum="0" Height="8" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
            <Button x:Name="CancelBtn" Content="취소" Click="OnCancel" />
            <Button x:Name="CloseBtn" Content="닫기" IsEnabled="False" Click="OnClose"
                    Style="{StaticResource AccentButtonStyle}" />
        </StackPanel>
    </Grid>
</Window>
