<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Nexa.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nexa.App"
    Title="Nexa Dir">
    <Grid Padding="12" RowSpacing="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Nexa Dir" FontSize="24" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" x:Name="StatusText" Text="스캐폴딩 — UI 셸 준비됨" Opacity="0.7" />
        <TextBlock Grid.Row="2" x:Name="DirHeader" Opacity="0.7" />

        <!-- 가상화 렌더: ItemsRepeater(저수준) + ScrollViewer. 인라인 트리 확장의 토대(docs/07 §7). -->
        <ScrollViewer Grid.Row="3" VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Auto">
            <ItemsRepeater x:Name="DirRepeater">
                <ItemsRepeater.Layout>
                    <StackLayout Orientation="Vertical" Spacing="2" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local:DirItem">
                        <Grid ColumnSpacing="12" Padding="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="64" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{x:Bind KindLabel}" Opacity="0.6" />
                            <TextBlock Grid.Column="1" Text="{x:Bind Name}" />
                            <TextBlock Grid.Column="2" Text="{x:Bind SizeLabel}" Opacity="0.6" />
                        </Grid>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Window>
