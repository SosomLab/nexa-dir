<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Nexa.App.BottomDockView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 콘텐츠 종류 선택(정보/미리보기/Hex/터미널) — 라디오식 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="2" Padding="6,4,6,2">
            <ToggleButton x:Name="KindInfo" Content="정보" Tag="Info" IsChecked="True" Click="OnKindClick"
                          FontSize="12" Padding="8,2" MinWidth="0" MinHeight="0" />
            <ToggleButton x:Name="KindPreview" Content="미리보기" Tag="Preview" Click="OnKindClick"
                          FontSize="12" Padding="8,2" MinWidth="0" MinHeight="0" />
            <ToggleButton x:Name="KindHex" Content="Hex" Tag="Hex" Click="OnKindClick"
                          FontSize="12" Padding="8,2" MinWidth="0" MinHeight="0" />
            <ToggleButton x:Name="KindTerminal" Content="터미널" Tag="Terminal" Click="OnKindClick"
                          FontSize="12" Padding="8,2" MinWidth="0" MinHeight="0" />
        </StackPanel>

        <!-- 콘텐츠(종류별 스왑) — 지금은 정보만 실제, 나머지는 준비 중 -->
        <ScrollViewer Grid.Row="1" Padding="8,2,8,6" HorizontalScrollMode="Auto" HorizontalScrollBarVisibility="Auto">
            <TextBlock x:Name="ContentText" FontSize="12" Opacity="0.8" TextWrapping="Wrap"
                       IsTextSelectionEnabled="True" />
        </ScrollViewer>
    </Grid>
</UserControl>
